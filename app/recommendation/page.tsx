import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Recommendation Letter | Luigi Espinosa - Frontend Developer',
};

export default function Recommendation() {
  return <h1>Recommendation Letter</h1>;
}
