---
scope: Cuatro Ecosystem Layer - 8 repositories, 6 frontend frameworks, 5 backend languages, one 2vCPU VPS
purpose: build-substrate
altitude: feature (keeps the seven epics of PRD 10 coherent)
updated: 2026-08-15T18:52
---

- (event) run start - hybrid coaching mode chosen by user; spine is the only deliverable (no extra renderings)
- (constraint) INHERITED from research: contracts federate, implementations do not. Tokens=format, OIDC=protocol, CI defs=reference. No cross-framework component library - a decision, not a deferral (PRD sec.8)
- (constraint) INHERITED: Anchored Hub. cuatro-portfolio = Turborepo monorepo of the Next.js cluster AND publisher of shared contracts. Turborepo stays strictly inside the JS/TS boundary
- (constraint) INHERITED: estate 15->8. Waypoints 12 (archive 3 shells) and 11 (absorb connect-four-react). End state = Anchor + 7 satellites
- (constraint) INHERITED topology: Traefik v3.7, subdomain routing, one Postgres with one db + one role per app, build-in-CI push-to-GHCR, docker-rollout gated on real healthchecks, pg_dump + restic
- (constraint) INHERITED identity: Clerk, OIDC Auth Code + PKCE per app, per-app __Host- session (never a domain-scoped cookie), RP-Initiated + Back-Channel logout, live_socket_id broadcast in cs-tracker. MaiCoin structurally exempt (wallet auth). ForwardAuth gates only the Traefik dashboard/admin surfaces
- (constraint) INHERITED token contract (UX DESIGN.md): three files - tokens.css (values only, :root), fonts.css (@font-face, relative urls), tailwind.css (generated @theme inline adapter). --token-* semantic layer, never colliding with Tailwind --color-*. Style Dictionary >=5.5.1, DTCG 2025.10
- (constraint) INHERITED (O-7 CLOSED): cs-tracker is on phoenix ~>1.8.7 with Tailwind v4 + daisyUI vendored. It takes the Tailwind ADAPTER route, not plain CSS. It is a Tailwind-cluster member for token purposes
- (constraint) INHERITED hard constraints: solo maintainer indefinitely (NFR-1); 2 vCPU/8GB unproven (NFR-3, SM-C4 wins every conflict); four live subdomains must never break (NFR-2); no unattended automation without a test suite (NFR-10); crawler exposure managed before the Registry ships (NFR-7)
- (note) brownfield fact: deploy.yml runs 'docker compose up --build -d' over ssh ON THE VPS - the box compiles today. Step name still says 'Deploy to Hetzner'. This is research's named top unmeasured risk, live now
- (note) brownfield fact: docker/Caddyfile routes only cuatro.dev + analytics.cuatro.dev yet binds :80/:443. The three other live subdomains route from something this repo does not describe - real deployed topology is not captured in the Anchor
- (note) brownfield fact: anchor is Next.js 16.1.6, React 19.2.4, Sass 1.97.3, pnpm 10.31.0, no Tailwind. pnpm-workspace.yaml exists but declares only onlyBuiltDependencies - no packages: key, so it is NOT a workspace root yet
- (note) brownfield fact: content/projects.ts has 1 entry (digital-library) with tech listing 'Hetzner VPS' and 'SQLite' already correct on SQLite - the FR-9 defect is the Hetzner value, and PRD FR-9 wrongly implies the stack itself was misrecorded
- (decision by user) PRD Q4 CLOSED - App Registry published form: registry.json is the AUTHORED source of truth, not an emitted artifact. A JSON Schema fixes the shape and gives editor + CI validation (FR-6 fails the build). TS types derive from the schema. Hub imports the same JSON it publishes (FR-12 no-second-copy). Satellites GET it at a stable URL at BUILD time, not runtime
- (decision by user) PRD Q1 CLOSED - Capacity Gate is a machine-readable file in the Anchor (baseline, written threshold, current reading, status, named overflow path, placement log). Deploy pipeline reads it: NEW service placement fails while status=blocked; existing services always deploy (NFR-2). Box 15-min loadavg is the gate; per-container cpu.pressure is the diagnostic that names the offender
- (decision by user) PRD Q3 CLOSED - Demo reset is ONE contract every stack implements: a single well-known demo principal owns all demo rows; an idempotent demo:reset command deletes-by-owner then reseeds a baseline fixture committed in the app repo; scheduled hourly with per-app override; the demo principal cannot be deleted or have credentials changed
- (decision by user) Anchor layout: end state is apps/* + packages/*, but the Hub moves into apps/hub as its OWN isolated step before any merge lands. Isolates the path-rewrite risk (ci.yml, lighthouse.yml, deploy.yml, docker/Dockerfile, tsconfig, vitest) from merge risk. NFR-2 honoured per step
- (decision by user) Merge history: git-filter-repo --to-subdirectory-filter on a scratch clone of each source, then merge --allow-unrelated-histories. Every historical commit already carries apps/<name>/ paths so git log --follow works from the new path. Hash rewriting is free because the source repos are archived. Chosen over git subtree because Marcus (UJ-2) reads commit history
- (decision by user) FR-19 token version record: token_contract field on each Registry Entry, verified by the SAME scheduled job as FR-32 link checking - fetch each satellite's vendored tokens.css from GitHub raw, parse the 'Contract vX.Y.Z' header the UX contract already specifies, compare, notify on mismatch. Declaration cannot drift from reality silently
- (decision by user) O-8/O-9 verification: Playwright added to the Anchor. O-8 asserted precisely via boundingBox() >= 44x44 on every interactive element at 360px. O-9 asserted STRUCTURALLY - the four Status values must differ in border-style, not only colour - which is the property the UX run actually fixed. Rejected extending Lighthouse tap-targets because 48x48/overlap is adjacent to O-8, not identical, and asserting a nearby property is the exact failure the UX validation named
- (note by user) corrected earlier claim: pnpm build DOES run in CI, inside lighthouse.yml, which builds and drives real Chrome against 3 URLs with accessibility asserted >=0.95 as an error. What is missing from CI is image production, not the build. Playwright is only a transitive pnpm-lock entry, not a devDependency - addendum C.2's 'Vitest + Playwright' overstates the installed harness
- (decision by user) Contracts boundary made enforceable by SHAPE: a top-level contracts/ directory is the entire published surface, and CI fails if any .ts/.js/.tsx/.mjs/.cjs file appears under it. The Style Dictionary generator lives in packages/tokens and is never published. Served at https://cuatro.dev/contracts/*. The Anchor's dual role becomes visible in the tree and the rule is one grep
- (decision by user) PRD Q11 CLOSED - staleness recorded as an AD that binds Epics 4-7: an epic whose first story opens after 2026-11-15 must record a refresh check first, and the SCOPE of that check is fixed by the AD (proxy/db/backup versions, Clerk + Railway pricing, Style Dictionary floor, LE lifetime schedule). Nothing else re-opens - a bounded check, not 'reread the research'
- (question) OPEN - how the three non-Anchor live subdomains route today is unknown to the Operator. docker/Caddyfile in the Anchor routes only cuatro.dev + analytics.cuatro.dev while binding :80/:443. Must be enumerated on the box BEFORE Epic 4 (greenfield rebuild), because the rebuild has to preserve four live subdomains it cannot currently enumerate from source
- (decision) digital-library is a DECLARED exception to one-Postgres-one-db-per-app: it runs SQLite + Redis. Consequence architecture must carry - the pg_dump + restic backup design does not reach its data, so a non-Postgres store carries its own declared backup path or its data is unbacked. An undeclared exception is a bug (same principle as FR-24/MaiCoin)
- (decision) O-3 folded in rather than left open: the token contract's Phoenix route specifies BOTH paths - @plugin daisyui/theme with var() if the scratch mix phx.new test passes, plain [data-theme] CSS if not. The test is a gate on Epic 1 Step 2, not a blocker on the contract, because the fallback is documented and equivalent in effect
- (decision) Each application is ONE independent deploy unit. The Anchor is one repository producing FOUR images. A Hub change must not redeploy finance. Per-app Dockerfile, GHCR image, compose service and Traefik router, all keyed on the application id
- (decision) One environment: production, plus local dev. No staging - NFR-1 and NFR-3 both refuse it. CI is the only pre-production gate, which is why the CI gates (typecheck, unit, Lighthouse a11y >=0.95, Playwright floor, registry schema, contracts purity) carry the whole weight
- (version) verified 2026-08-15 on the web: Traefik v3.7.10 (2026-07-31) - matches research; Turborepo 2.10.10; Style Dictionary 5.5.1 confirmed via registry.npmjs.org (a search summary claiming 5.5.0 was wrong - 5.5.1 is the security floor, prototype pollution in convertTokenData); restic 0.19.1 (2026-07-05, research said 0.19.0); docker-rollout v0.14 (2026-07-12, still one release in trailing 12mo - research's cadence caveat stands)
- (version) verified 2026-08-15: PostgreSQL 18.6 current stable (2026-08-13); PostgreSQL 19 GA expected September 2026. Anchor's compose runs postgres:16-alpine today. Greenfield target (Epic 4) is chosen at that time under the staleness AD, since 19 lands right about when Epic 4 plausibly runs
- (version) verified 2026-08-15: @playwright/test 1.62.1 current. Repo pins from package.json: Next.js 16.1.6, React 19.2.4, Sass 1.97.3, pnpm 10.31.0, Vitest 4.0.18, three 0.183.2
- (event) reviewer gate run: lint 0 findings, then 3 lenses (rubric walker + the 2 configured reviewers) run sequentially inline - subagent dispatch is barred by standing session instruction. Reviews in reviews/
- (decision) GATE CRITICAL fixed - AD-19 asserted the four Status values differ in border-style, which CONTRADICTS EXPERIENCE.md: Live and Complete are both 1px solid and differ only by the 4px dot (1.13:1 in greyscale without it). The assertion as drafted would fail a correct implementation and pass a broken one - the exact failure the UX validation named. AD-19 now binds to the UX spec's three structural axes and forbids a border-only assertion
- (decision) GATE HIGH fixed - new AD-23: migrations never run on container boot, run as a discrete pre-rollout step, and must be backward-compatible with the version still serving. docker-rollout is scale-then-drain so old and new containers overlap by construction; migrate-on-boot (the Prisma default shape) would break live requests for the length of the drain, silently, with no user to report it
- (decision) GATE HIGH fixed - AD-14 now names the vendored folder cuatro-contracts/. Without a fixed name, seven satellites with seven stylesheet layouts all comply with 'copy the folder' and AD-16's verification job cannot locate tokens.css in any of them - FR-19 would go unmet while appearing satisfied
- (decision) GATE fixes applied: NFR-8 (first-party measurement only) and NFR-4 (recurring cost ceiling) had no home in the spine - both now Consistency Conventions rows. Internal contradiction resolved between 'never latest' and Umami's postgresql-latest. Node pinned to 24 LTS (22 has been maintenance-only since 2025-10-21). AD-8 fixes the monorepo Docker build context to turbo prune --docker. AD-13 derives the demo principal as demo@cuatro.dev and puts the scheduler on the host. AD-4 states the accepted build-time propagation staleness
- (event) spine finalized
