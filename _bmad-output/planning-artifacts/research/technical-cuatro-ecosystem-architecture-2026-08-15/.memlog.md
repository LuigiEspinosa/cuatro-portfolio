---
topic: Cuatro Ecosystem architecture
type: technical
decision: Source layout, deployment topology, and design-system strategy for unifying 15 polyglot personal projects into one suite on a single VPS
preset: deep
updated: 2026-08-15T15:17
---

- (decision) Plan gate approved: technical/select, breadth-first, 4 dimensions (D1 polyglot monorepo economics, D2 multi-framework design systems, D3 single-VPS topology, D4 devcontainers+Windows relocation). Preset upgraded standard->deep (6 subagents, 12 sources/round, 3 rounds) and red_team off->on at user request, to satisfy the ask that option 3 be attacked as hard as the others.
- (decision) Sub-questions 5 (tracker consolidation) and 6 (migration sequencing) excluded from web research: they are judgments over the requester's own project facts, not web-answerable. Derived at synthesis from D1-D4 verdicts and marked inferred.
- (decision) Tension resolved: prompt forbids big-bang cutover, user later stated wiping the VPS is acceptable. Reconciled as source-layout migration must be incremental; host may be rebuilt greenfield. No project has real users, so deploy-target churn is cheap.
- (assumption) Binding constraint taken to be 2 vCPU, not 8 GB RAM. Hostinger KVM 2 is RAM-generous and CPU-poor; the load case is 4 Next.js SSR runtimes + BEAM + Fastify + Python + Go on two shared cores. D3 must argue against CPU and name the outgrow threshold. To be validated against measured footprints in D3.
- (assumption) Subdomain routing is the incumbent, not an open field: cuatro.dev, cs-tracker.cuatro.dev, tracker.cuatro.dev, library.cuatro.dev are already live under one apex. Path routing must beat a working incumbent to win.
- (event) Round 1 fan-out launched: 6 researchers, breadth-first. D1a polyglot monorepo tooling, D1b monorepo/polyrepo evidence + solo discount, D2a multi-framework design system precedents + token ceiling, D2b token contract mechanics, D3a reverse proxy selection, D3b single-box capacity + DB topology. 6 sources / 15 tool calls each.
- (assumption) Additional constraints supplied mid-run by user: cuatro.dev registered at Squarespace; DNS managed by Cloudflare; user holds existing Supabase and Railway accounts. Implications to test: Cloudflare DNS makes DNS-01 wildcard ACME trivial and is first-class in Traefik/Caddy; Cloudflare proxy mode interacts with TLS termination and with Phoenix LiveView WebSockets; Railway is a live alternative deploy target that must be screened as a candidate rather than ignored; Supabase account means managed-Postgres is available at no migration cost.
- (source) D1a r1 batch: Turborepo repo tagline, Nx 2026 roadmap (2026-02-04), Nx plugin registry, moonrepo maintainer statement (2024-09-04), Pants 2.33 backend docs, arXiv 2510.20041 Kubernetes Bazel->Make (2025-10-24), Elixir Forum thread (2025-11-04). Digest: digests/D1a-r1-1.md
- (claim) ref=[D1a-1] status=verified class=ecosystem pub=2026-08 — Turborepo is JS/TS-only by its own project description; non-JS languages can only be invoked as opaque shell tasks, so no cross-language caching or graph awareness.
- (claim) ref=[D1a-2] status=verified class=ecosystem pub=2026-02 — Nx's own roadmap concedes polyglot repos require every language toolchain installed merely to compute the project graph; the lazy-hydration fix is roadmap, not shipped.
- (claim) ref=[D1a-3] status=verified class=ecosystem pub=2026-02 — Nx ships non-JS plugins for Docker, .NET and Maven only; Python is planned for 2026 and Go, Elixir and Solidity appear nowhere with no dated commitment.
- (claim) ref=[D1a-4] status=verified class=ecosystem pub=2025-11 — No credible Elixir/Mix integration exists in any polyglot orchestrator: no open-source Bazel Elixir ruleset, and practitioners report deliberately keeping Mix tooling separate.
- (claim) ref=[D1a-5] status=verified class=failure pub=2025-10 — Kubernetes migrated off Bazel to Make, citing complexity, onboarding and maintenance burden rather than capability limits (arXiv 2510.20041). Quantified penalties not verified and not relied on.
- (question) OPEN from D1a: did moon v2.0 (2026-05, WASM plugin toolchains) change the 2024 maintainer position that Python/Go get no integration beyond PATH execution? This is the single biggest unresolved gap in D1 and is queued for round 2.
- (source) D1b r1 batch: Proton monorepo migration post, Ken Muse anti-monorepo case, Streamdal monorepo consolidation (2024-01-03), Grab git scale report (2025-09-16), Losoviz multi-monorepo CSS-Tricks (2021). Digest: digests/D1b-r1-1.md
- (claim) ref=[D1b-1] status=verified class=team-dependent pub=2024 — Both the mainstream pro- and anti-monorepo arguments are team arguments; Proton's cited benefits and Ken Muse's cited costs alike dissolve at N=1, so neither binds a solo maintainer.
- (claim) ref=[D1b-2] status=verified class=pattern pub=2024-01 — Streamdal merged ~10 repos into a monorepo for PERCEPTION reasons (stale 'last updated' on the flagship repo, wrong GitHub language bar, users unable to tell which repo to file against), not engineering economics; the same author still calls monorepos 'mostly terrible' generally.
- (claim) ref=[D1b-3] status=verified class=performance pub=2025-09 — Git scale pain is driven by commit/ref/history count, not project count: Grab's 214 GB repo with ~13M commits and 12.2M refs took 7.9 min to clone, and 99.9 pct of their improvement came from deleting commits and refs. Fifteen personal projects cannot approach these thresholds.
- (claim) ref=[D1b-4] status=unverified class=failure pub=2021 — The documented failure of the multi-monorepo (hub-and-satellite) pattern is that shared workflows had to be manually copied downstream despite the hub being declared source of truth. Predates mature GitHub Actions reusable workflows; currency NOT established and queued for round 2.
- (claim) ref=[D1b-5] status=verified class=cost pub=2024 — Layout reversibility at 10-15 repos costs roughly one focused week (Proton weekend cutover, Streamdal ~1 week), making the source-layout decision cheap to reverse once and expensive to reverse repeatedly.
- (claim) ref=[D1b-6] status=verified class=ecosystem pub=2026-08 — There is ZERO published study on solo or very-small-maintainer repository layout. Reported as a genuine evidence gap, not a search failure; all solo-specific conclusions in this report are therefore reasoned from team evidence with the team-dependent parts subtracted.
- (source) D2a r1 batch: Material Web maintenance-mode announcement (2024-06-10), Adobe Spectrum maintainer statements, IBM Carbon framework-support docs, React 19 custom-elements support notes + DSD hydration bug (2025-07-04). Two fetches degraded (403 / no disclosure). Digest: digests/D2a-r1-1.md
- (claim) ref=[D2a-1] status=verified class=failure pub=2024-06 — Google placed Material Web, the best-resourced attempt at one Lit-based component set serving all frameworks, into maintenance mode on 2024-06-10, reassigning engineers to its internal Wiz framework; new components are not planned and PRs are not accepted by default.
- (claim) ref=[D2a-2] status=verified class=pattern pub=2026-08 — Adobe deliberately maintains BOTH React Spectrum/React Aria and Spectrum Web Components plus swc-react wrappers, with maintainers citing React-lifecycle-specific API decisions and no consolidation plan; duplication is a paid-for choice, not an oversight.
- (claim) ref=[D2a-3] status=verified class=pattern pub=2026-08 — Every multi-framework design system verified this run resolves to the same shape: web-component core plus per-framework wrappers plus community ports for the tail. No system ships one component set consumed natively by six frameworks.
- (claim) ref=[D2a-4] status=unverified class=version pub=2025-07 — React 19 fixed client-side custom-element props and events, but React still does not convert template shadowrootmode into a shadow root, producing SSR hydration errors; bug closed as stale with no maintainer reply. SINGLE SOURCE, two-source class not met, queued for round 2.
- (claim) ref=[D2a-5] status=verified class=pattern pub=2026-08 — Tokens federate VALUES (color, type, spacing, radii, elevation, motion). Nothing retrieved shows tokens federating BEHAVIOUR (focus management, form participation, overlays, dense data UI). This is the evidenced shape of the token-only ceiling.
- (question) OPEN from D2a: no published numbers exist on the N-framework maintenance multiplier (that search surface is agency marketing only); no first-hand retrospective found on where token-only coherence breaks. Cost evidence is behavioural (Google defunding, Adobe double-paying) rather than numeric. Reported as thin.
- (source) D2b r1 batch: W3C DTCG Design Tokens Format Module 2025.10 (2025-10-28), Style Dictionary npm/GitHub releases, Terrazzo token-tools 2.2.0, Tailwind v4.3 theme docs, NL Design System versioning convention. Digest: digests/D2b-r1-1.md
- (claim) ref=[D2b-1] status=verified class=version pub=2025-10 — The W3C DTCG Design Tokens Format Module reached its first STABLE version 2025.10 on 2025-10-28, covering theming, multi-brand, Oklch/Display-P3 and aliases; it is safe to build a token contract on. Resolver stability was not addressed in the announcement.
- (claim) ref=[D2b-2] status=verified class=version pub=2026-08 — Style Dictionary 5.5.1 is current, defaults to DTCG JSON output, targets the 2025.10 spec, and ships roughly monthly; 5.5.1 patched a prototype-pollution vulnerability in convertTokenData so pinning below it is unsafe. Exact release DATES are unverified (a source returned impossible 2024 stamps) and must be re-checked.
- (claim) ref=[D2b-3] status=verified class=cost pub=2026-08 — Token semver convention: a color VALUE change is a minor bump, a token RENAME is major. Contract changes break, pixel changes do not. Without atomic cross-repo commits the only workable model is deprecate, migrate, remove.
- (claim) ref=[D2b-4] status=unverified class=pattern pub=2026-08 — Under Tailwind v4 theme-based config, a plain shared CSS custom-properties file supplies variables but generates NO utility classes unless values are re-declared in @theme. INFERRED from the documented @theme-vs-:root distinction, single source, flagged for verification. Material to the token contract if true.
- (question) OPEN and IMPORTANT from D2b: ZERO published case studies name any real design system's cross-repo token DISTRIBUTION mechanism. None of npm, CDN, submodule, subtree, copy-bot or Renovate was evidenced in practice. Partially covered by the D1 round-2 agent researching reusable workflows and centralized config distribution. Also: no published precedent anywhere of a non-JS app consuming a JS-ecosystem token package - Phoenix would be inventing this.
- (source) D3a r1 batch: Traefik v3.7.10 release notes (2026-07-31), Caddy v2.11.4 (2026-06-03), Let's Encrypt rate-limit page (updated 2026-08-05), LE certificate-lifetime policy, Cloudflare WebSocket docs (updated 2026-08-14), Justin Duke subpath retrospective (2026-02-18). Digest: digests/D3a-r1-1.md
- (claim) ref=[D3a-1] status=verified class=version pub=2026-07 — Traefik v3.7.10 (2026-07-31, near-weekly point releases across three branches) and Caddy v2.11.4 (2026-06-03, roughly monthly) are current. nginx 1.30.0 is aggregator-sourced only and unverified.
- (claim) ref=[D3a-2] status=verified class=pattern pub=2026-08 — nginx is cut on TLS grounds: no built-in ACME client and it does not reload itself after renewal, a silent-failure surface that worsens as certificate lifetimes shorten.
- (claim) ref=[D3a-3] status=verified class=cost pub=2026-08 — Let's Encrypt certificate lifetime moves 90 days to 64 days (Feb 2027) to 45 days (Feb 2028), with the tlsserver profile already at 45 days since 2026-05-13. Renewals stay exempt from rate limits, but the window between a silently broken renewal and a down site halves; alerting on certificate AGE becomes mandatory.
- (claim) ref=[D3a-4] status=verified class=failure pub=2026-02 — Buttondown's CEO, eight years in, publicly regrets path-based routing: shared browser origin means cookies, localStorage and XSS cross every app, plus shared SEO blast radius and namespace conflicts. No account of anyone regretting SUBDOMAINS was found, though only one search was run for it.
- (claim) ref=[D3a-5] status=disputed class=performance pub=2026-08 — The widely-cited 100-second Cloudflare Free/Pro WebSocket idle timeout does NOT appear in Cloudflare's own documentation (page updated 2026-08-14); Cloudflare publishes only a keepalive recommendation. Long-lived WebSocket disconnect reports behind the proxy are a real recurring class. Grey-clouding the LiveView subdomain removes the question entirely.
- (event) D3a REFUSED to report proxy memory footprints: every available figure (85/120/180 MB) traced to AI-generated SEO content farms recycling each other, failing the two-source bar. Recorded as a measurement task on the actual VPS rather than a cited claim. This is the correct outcome, not a gap.
- (source) D3b r1 batch: vercel/next.js discussion 81967 (2025-07 to 2026-02), Postgres shared_buffers docs, Prisma connection-limit guidance, Supabase pricing page (read 2026-08-15), Railway pricing page + 4 corroborating sources (read 2026-08-15), self-hosted Supabase requirements. Digest: digests/D3b-r1-1.md
- (claim) ref=[D3b-1] status=verified class=performance pub=2026-02 — A single Next.js 15.4.3 'next start' process was measured at 639,880 KB RSS and 103.3 pct CPU, i.e. one container consuming half of a 2-vCPU box. Contested thread, no maintainer fix version. This is the ONLY hard footprint number obtainable; the two-source bar was met zero times for BEAM, Fastify and Traefik idle RSS, which are simply not published in citable form.
- (claim) ref=[D3b-2] status=verified class=performance pub=2026-02 — The trigger for that Next.js CPU spike was BOT CRAWLERS, not human users, and Cloudflare filtering cut it by over 90 pct. For a portfolio box with no real users this is the dominant load profile, making a bot filter the cheapest CPU remedy rather than tuning or a bigger box.
- (claim) ref=[D3b-3] status=verified class=pattern pub=2026-08 — Database call: ONE Postgres container with one database and one role per app. Each additional Postgres instance pre-commits its own shared_buffers (128 MB default), so five containers reserve roughly 640 MB before serving a query. Schema-per-app is rejected because it couples Prisma migrations across apps.
- (claim) ref=[D3b-4] status=verified class=cost pub=2026-08 — Supabase free tier pauses projects after 7 days of inactivity, caps at 500 MB and 2 projects, and includes no backups; Pro is 25 USD/mo and never pauses. A low-traffic portfolio app is precisely the workload the free tier pauses.
- (claim) ref=[D3b-5] status=verified class=cost pub=2026-08 — Railway prices at 20 USD/vCPU/mo, 10 USD/GB RAM/mo and 0.05 USD/GB egress, Hobby 5 USD/mo including 5 USD credit. Memory dominates for idle apps: one idle Next.js at 640 MB is about 6.40 USD/mo in RAM alone, so all 12 apps would be roughly 61 USD/mo memory-only, at or over the stated ceiling. Partial offload of 2-3 apps lands around 15-30 USD/mo.
- (claim) ref=[D3b-6] status=verified class=pattern pub=2026-08 — Self-hosting Supabase requires 10+ containers and 8 GB RAM for itself alone, which consumes the entire box. The three-way database comparison therefore collapses to managed Supabase versus plain Postgres on the VPS.
- (claim) ref=[D3b-7] status=unverified class=performance pub=2026-08 — Predicted failure order on the box: 15-minute load average past 2.0 while RAM is still fine, then Node event-loop delay and ResponseAborted errors, then p99 degradation across UNRELATED apps sharing the cores. Top unmeasured risk is running 'next build' on the box while serving from it. Reasoned from the single measured datapoint, not independently evidenced.
- (question) OPEN from D3b, out of budget not out of sources: backups (pgBackRest/wal-g/restic/borg/provider snapshots) effectively unanswered; PSI and load-average outgrow thresholds unanswered; Docker Compose deploy.resources behaviour outside Swarm is issue-tracker hearsay and undated, must be verified against official docs before any compose file is written; Railway sleep policy unverified; BEAM footprint unknown. Queued for D3 round 3.
- (decision) CONSTRAINT RELAXED mid-run by user: open to migrating A project to a different stack to make the ecosystem work, but explicitly values variety across stacks. Reframes non-goal 2: wholesale rewrite to one framework stays rejected, but selective single-project migration is now admissible. Note the interaction with ranked criterion 2 (portfolio value): stack variety is an ASSET to a hiring-manager audience, so heterogeneity should be treated as deliberate range rather than as debt to be paid down. Any consolidation call must justify itself against lost portfolio breadth, not only against maintenance cost.
- (source) D1 r2 batch: moon .moon/toolchains docs (accessed 2026-08-15), moon v2.0 announcement, GitHub Docs reusable workflows, git scalar docs (2.53.0), GitHub clone-strategy guidance. Digest: digests/D1-r2-1.md
- (claim) ref=[D1r2-1] status=verified class=ecosystem pub=2026-08 — moon v2 now ships Go and Rust as stable first-class toolchains and Python only as unstable_python/unstable_pip/unstable_poetry/unstable_uv; Elixir and Solidity are absent entirely. The 2024 'PATH only' complaint is therefore wrong for Go, half-wrong for Python, and still fully true for Elixir and Solidity.
- (claim) ref=[D1r2-2] status=unverified class=version pub=2026-08 — moon's version is UNRESOLVED and fails the two-source rule: the announcement dates v2.0 to 2026-02-18 while a secondary source sits at a 2026-05 URL, and a search snippet says 2.4.6 while docs badge a feature as added in 2.5.0. No moon version may be quoted downstream without reading the releases page directly.
- (claim) ref=[D1r2-3] status=verified class=pattern pub=2026-08 — The 2021 hub-and-satellite failure (workflows manually copied downstream) is OBSOLETE for the workflow body: GitHub Actions supports cross-repo reusable workflows referenced as owner/repo/.github/workflows/file@ref, private repos included, pinnable to a SHA. Residue is one small caller stub per satellite. Documented limits (10 nesting levels, no environment secrets via workflow_call, permissions may only narrow) do not bind a solo developer.
- (claim) ref=[D1r2-4] status=verified class=performance pub=2026-08 — Modern git scale tooling is a NON-FACTOR at this scale: a ~15-project personal repo would never need partial clone, sparse checkout, scalar or manual commit-graph work. GitHub's own guidance recommends full clones for reasonably-sized repositories. This argues neither for nor against a monorepo here.
- (decision) D1 dimension CLOSED on coverage after 2 rounds: the three round-2 questions are answered. Net effect on candidates: option 1 (full monorepo) loses its tooling rationale entirely (no orchestrator covers Elixir/Solidity, modern git offers no help at this scale), while option 3 (hub + satellites) gains materially because its documented central failure mode is now fixed by reusable workflows.
- (source) D2 r2 batches: primer/view_components README (2026-02), @primer/primitives, caniuse + Baseline DSD data, facebook/react issues 33698 (2025-07-04) and 26071, lit-labs/ssr 4.1.0, Phoenix LiveView v1.2.9 js-interop docs, Phoenix 1.8.0 release (2025-08-05). Digests: digests/D2-r2-1.md, digests/D2-r2-2.md
- (claim) ref=[D2r2-1] status=verified class=failure pub=2026-02 — GitHub's primer/view_components entered maintenance mode in February 2026, citing GitHub's move to React-based interfaces, taking no new components and directing internal consumers to Primer React. The canonical server-rendered-plus-React design-system precedent was resolved by DELETING the server-rendered half, not by unifying it.
- (claim) ref=[D2r2-2] status=verified class=pattern pub=2026-08 — Primer shares TOKENS and only tokens: @primer/primitives is JSON built with Style Dictionary and distributed as themed CSS custom-property files, feeding @primer/react, @primer/view_components and @primer/css independently; components and behaviour are reimplemented per stack. This is the strongest published confirmation of the token-only ceiling.
- (claim) ref=[D2r2-3] status=verified class=version pub=2026-08 — Declarative Shadow DOM shipped in Chrome/Edge 111 (2023-03), Safari 16.4 (2023-03-27) and Firefox 123 (2024-02-20); Baseline 'newly available' since 2024-02-20 and NOT yet 'widely available' as of 2026-08-15, with the 30-month flip due around 2026-08-20. caniuse reports 94.27 pct global support. Two sources agree exactly.
- (claim) ref=[D2a-4] status=verified class=version pub=2025-07 — CORROBORATED and status upgraded from unverified: React will not convert template shadowrootmode into a shadow root, causing SSR hydration mismatches; facebook/react 33698 (2025-07-04, React 19.0/19.1) plus 26071 (React 18.2). Caveat: 33698 was closed as not planned and labelled stale/unconfirmed with no maintainer reply, so this is corroborated as reported-and-unfixed, not React-acknowledged. Workaround is hydrateShadowRoots() from webcomponents/template-shadowroot.
- (claim) ref=[D2r2-4] status=verified class=version pub=2025-09 — Form-associated custom elements are Baseline WIDELY AVAILABLE since 2025-09-27 (Chrome 77, Edge 79, Firefox 98, Safari 16.4). Repeating form participation as a web-components blocker in 2026 would be a false claim. Single-source under this run's two-source bar; flagged.
- (claim) ref=[D2r2-5] status=verified class=version pub=2026-05 — Lit SSR remains pre-1.0 in Labs (@lit-labs/ssr 4.1.0, roughly 2026-05), self-described as pre-release that may break or be dropped. Server-side rendering of web components is still not a settled foundation.
- (claim) ref=[D2r2-6] status=verified class=ecosystem pub=2025-08 — Phoenix 1.8.0 (2025-08-05) ships Tailwind v4 plus daisyUI with a light/dark toggle in the layout and deliberately TRIMMED core_components.ex to only the main building blocks. Every Phoenix UI library surfaced (SaladUI, Petal, PrimerLive, Fluxon, Mishka Chelekom) is HEEx plus Tailwind; none is web-component based. Phoenix consumes design as Tailwind classes and CSS custom properties, not as components.
- (claim) ref=[D2r2-7] status=verified class=pattern pub=2026-08 — Phoenix LiveView v1.2.9 documents three client-owned-DOM mechanisms: phx-update=ignore, phx-hook with a full lifecycle, and the LiveSocket dom option onBeforeElUpdated which the docs recommend for libraries needing broad DOM management. Notably the interop pages never say 'custom element', 'web component' or 'shadow DOM' - the story is generic client-side libraries and custom-element support is left to be inferred.
- (claim) ref=[D2r2-8] status=unverified class=ecosystem pub=2026-08 — No published design system serving BOTH a Phoenix/LiveView app and a JS-framework app was found across multiple searches. Genuinely unwritten territory; a token layer spanning LiveView and the JS cluster would be built without precedent to copy.
- (decision) D2 verdict reached with high confidence: TOKENS ONLY federate across the six frameworks. Evidence is behavioural and convergent - Google defunded Material Web (2024-06), GitHub retired Primer ViewComponents (2026-02), Adobe pays for duplicate React and web-component implementations by choice, and Primer shares tokens and only tokens. The optional per-cluster React component package remains viable because it is single-framework and sidesteps every cross-framework failure mode.
- (source) D3 r2 batch: Caddy module page, Traefik v3.7 ACME docs, Cloudflare SSL modes + WebSockets docs, Dokploy GitHub releases (v0.30.0 2026-08-14), Phoenix assets/js/phoenix/socket.js. D4 r1 batch: containers.dev spec + issue 159, @devcontainers/cli 0.88.0, Microsoft WSL docs (updated 2026-06-02), VS Code containers docs, git worktree/safe.directory docs. Digests: digests/D3-r2-1.md, digests/D4-r1-1.md
- (claim) ref=[D3r2-1] status=verified class=version pub=2026-08 — Caddy's Cloudflare DNS module is not bundled: Caddy's own module page states it does not come with Caddy and must be added via xcaddy or the download page, with no official image, no caddy add-package and no dynamic loading. The rebuild-on-every-upgrade friction is real.
- (claim) ref=[D3r2-2] status=verified class=version pub=2026-08 — Traefik v3.7 configures ACME DNS-01 through Lego via environment variables with no install or build step, so Cloudflare DNS-01 works on the stock image. Confidence medium-high; Cloudflare is not named inline on the page read.
- (claim) ref=[D3r2-3] status=verified class=performance pub=2026-08 — Phoenix sets heartbeatIntervalMs to 30000 by default in assets/js/phoenix/socket.js, so a LiveView socket pings every 30 seconds and any proxy idle timeout above 30 s never fires. This retires the Cloudflare WebSocket concern for LiveView. Cloudflare confirms WebSockets on all plans and deliberately does not publish its idle-close period.
- (claim) ref=[D3a-5] status=verified class=performance pub=2026-08 — Status updated from disputed: the 100-second Cloudflare WebSocket figure is confirmed absent from Cloudflare's documentation and no number is asserted. Combined with the 30 s LiveView heartbeat, the risk is retired regardless of the unpublished value.
- (claim) ref=[D3r2-4] status=verified class=pattern pub=2026-08 — Cloudflare documents FIVE SSL/TLS modes, not four. The redirect loop is bidirectional: Flexible combined with an origin HTTP-to-HTTPS redirect, and Full or Full-strict combined with an origin HTTPS-to-HTTP redirect. Origin CA certificates are not publicly trusted, pair with Full-strict, and send no expiry notifications, so they sit outside the shortening public-CA lifetime treadmill.
- (claim) ref=[D4-1] status=verified class=pattern pub=2026-08 — CONFIRMED false dichotomy: repository layout answers where code lives, devcontainers answer what toolchain a folder gets. The Dev Container spec explicitly supports multiple devcontainer.json files in one repository, so devcontainers WITH a monorepo is a documented pattern. The choice is a two-by-two, not an either/or.
- (claim) ref=[D4-2] status=verified class=pattern pub=2026-06 — The dominant avoidable Windows cost is repository PLACEMENT, not containerisation. Microsoft's own WSL documentation (updated 2026-06-02) recommends against working across operating systems and advises storing project files in the Linux filesystem rather than /mnt/c or C:. Moving repositories into the WSL2 ext4 filesystem is a bigger and cheaper win than adopting devcontainers. Neither Microsoft nor VS Code publishes NUMBERS for the penalty; third-party figures could not be corroborated and are not cited.
- (claim) ref=[D4-3] status=verified class=ecosystem pub=2026-06 — @devcontainers/cli is at 0.88.0 (June 2026) on a 2-4 week cadence and remains PRE-1.0 after years. Support is not editor-agnostic in practice: VS Code full, Visual Studio 2022 C++/CMake only, IntelliJ early-stage per containers.dev, and implementations diverge on which devcontainer.json properties they honour.
- (claim) ref=[D4-4] status=verified class=pattern pub=2026-08 — On relocation: plain repositories move cleanly. Worktrees BREAK because worktree.useRelativePaths defaults to false; the fix is git worktree repair. Expect safe.directory dubious-ownership errors (git 2.35.2, CVE-2022-24765) and MAX_PATH failures requiring core.longpaths plus separate OS-level long-path enablement.
- (question) OPEN from D4: four of eight relocation sub-items are UNEVIDENCED - IDE workspace absolute paths, Docker Compose bind mounts, core.filemode/symlinks, and OneDrive-synced folders. No published guidance exists on ordering a multi-repo relocation. No primary data at all on devcontainer rebuild times or disk consumption. These will be marked inferred in the report, not presented as researched.
- (question) OPEN from D3 r2: zero-downtime deploys on plain Compose remain partly unanswered. Docker's deploy spec documents update_config and order but does not state whether docker compose honours them outside Swarm, and no primary source confirms whether 'compose up' drops connections. Only credible pattern found is scale-2x-then-drain (docker-rollout), secondary evidence only. Dokploy v0.30.0 (2026-08-14, ~10 releases May-Aug 2026) is verified actively shipping; Coolify v4.3.x cadence NOT established. Dokku, CapRover and Kamal not obtained - that search tier was pure AI SEO content farm and no overhead numbers are given.
- (source) D3 r3 batch: Docker Compose services reference + resource-constraints page, PostgreSQL 18 backup docs (page dated 2026-08-13), Linux kernel PSI documentation, restic/Borg/pgBackRest/Barman release pages. D2 r3 batch: Renovate docs + releases (44.30.3, 2026-08-15), Dependabot/GitHub Docs, npm and GitHub Packages docs, GitHub template-repository docs. Digests: digests/D3-r3-1.md, digests/D2-r3-1.md
- (claim) ref=[D3r3-1] status=unverified class=version pub=2026-08 — Use service-level cpus: and mem_limit: to cap containers: both are documented in the current Compose services reference as ordinary service attributes with no Swarm qualifier, and the old 'ignored outside Swarm' warning is absent from current official pages. However NEITHER page positively states that deploy.resources.limits is enforced by docker compose up, so enforcement cannot be certified from documentation. cpu_shares is a relative soft weight and is the wrong key for capping. Settle empirically: run docker compose up with only deploy.resources.limits set, then docker inspect and read HostConfig.NanoCpus and HostConfig.Memory.
- (claim) ref=[D3r3-2] status=verified class=version pub=2026-07 — Backup tools live and shipping: restic 0.19.0 (2026-06-09), Borg 1.4.5 (2026-07-19, with 2.0 still beta and explicitly not for production), pgBackRest 2.59.0 (2026-07-20, single-source), Barman 3.14.0 (2025-05-15, slower cadence). wal-g liveness could NOT be established this run - no dated 2026 release retrieved - so it should not be adopted on this evidence.
- (claim) ref=[D3r3-3] status=verified class=pattern pub=2026-08 — PostgreSQL 18 documentation states a running server's data directory cannot be tar-copied, but a frozen snapshot IS usable and restores as a crash-recovered server replaying WAL, PROVIDED snapshots across all volumes are simultaneous. That is the documented hazard for multi-volume setups, and a provider snapshot is additionally not offsite from the provider. The docs call filesystem backups inferior to pg_dump. No official source says dump-only is inadequate at this scale, so no such claim is made: PITR buys a smaller loss window, not correctness.
- (claim) ref=[D3r3-4] status=verified class=performance pub=2026-08 — Pressure Stall Information is the right saturation instrument, exposing avg10/60/300 as percentages plus a total in microseconds, where 'full' means all non-idle tasks stalled. The kernel publishes NO universal trouble threshold, only a trigger format with worked examples of 150ms/1s (some, memory) and 50ms/1s (full, io). On Ubuntu 24.04's cgroup v2 the same data is available per-container via cpu.pressure, memory.pressure and io.pressure, which attributes pressure to a single app - that is the operationally important part.
- (claim) ref=[D2r3-1] status=verified class=version pub=2026-08 — Renovate 44.30.3 (2026-08-15, roughly 2-3 releases per day) supports shareable config presets via extends github>owner/repo, resolving to default.json on that repo's default branch and pinnable by tag as #1.2.3; the docs frame this explicitly as holding config in one location rather than copy-pasting to every repository. Dependabot has NO cross-repo config inheritance at all. This is the deciding feature for governing 15 repos from one hub.
- (claim) ref=[D2r3-2] status=verified class=version pub=2026-08 — FOLKLORE CORRECTED: Dependabot does now support grouping (groups with patterns, exclude-patterns, update-types, group-by). What it still lacks is shareable central config and any native automerge. Its only org-level centralization is private-registry credentials, GA 2025-07-22, with private repos gated behind the Code Security SKU.
- (claim) ref=[D2r3-3] status=verified class=cost pub=2026-08 — Publish the token package PUBLICLY to npmjs.com. GitHub Packages documentation states an access token is needed to publish, install and delete private, internal AND PUBLIC packages, which would mean an .npmrc token on every dev machine and every CI job across 15 repos. Public npm needs npm publish --access public, a free account and 2FA, with zero consumer auth.
- (claim) ref=[D2r3-4] status=verified class=pattern pub=2026-08 — GitHub template repositories are one-time scaffolds only: branches created from a template have unrelated histories and no propagation mechanism is documented. Useful for creating repo 16, useless for updating repos 1 to 15.
- (claim) ref=[D1r2-3b] status=overturned class=capability pub=2026-08 — CORRECTION to the earlier round-2 finding: organization rulesets are NOT Enterprise-gated (Free for public repos, Pro/Team/Enterprise Cloud for private, push rulesets needing Team or above). However rulesets govern branch and push PROTECTION, not file content, so they are not a config-propagation mechanism at all and the earlier hope that they could remove the per-repo caller stub is void. Rulesets tier claim is single-source from a search summary; re-verify before it is load-bearing.
- (decision) PROPAGATION MECHANISM RESOLVED, closing the hardest part of sub-question 2: hub publishes a scoped tokens package publicly to npmjs.com on tag via GitHub Actions; each of the 15 repos carries exactly two small frozen files, a one-line renovate.json extending github>owner/renovate-config and a reusable-workflow caller stub; a renovate-config repo holds default.json with all policy, grouping token updates and automerging minor/patch on green CI while leaving majors as PRs. Every future policy change is then one commit in the hub. This is the atomic-commit substitute.
- (source) D3 r3b batch: docker compose up CLI reference, docs.docker.com/reference/compose-file/deploy, upstream compose-spec deploy.md, wowu/docker-rollout releases (v0.14 2026-07-12), Coolify/Dokku/CapRover/Kamal release pages. Digest: digests/D3-r3-2.md
- (claim) ref=[D3r3b-1] status=verified class=pattern pub=2026-08 — Plain docker compose up DOES drop connections: Docker's compose up CLI reference states that when a service's config or image changed, up picks up the changes by STOPPING AND RECREATING the containers. Stop-first by construction; the page never mentions zero-downtime, rolling updates or start-first.
- (claim) ref=[D3r3b-2] status=verified class=version pub=2026-08 — deploy.update_config.order outside Swarm is a DOCUMENTED ABSENCE, not a search failure: both docs.docker.com/reference/compose-file/deploy and the upstream normative compose-spec deploy.md document order start-first|stop-first with zero runtime scoping and neither mentions Swarm versus compose up; the only adjacent line is the generic 'deploy is optional and may be ignored'. Treat the deploy key as inert under compose up. Two authoritative documents read directly.
- (claim) ref=[D3r3b-3] status=verified class=pattern pub=2026-08 — docker compose --wait means wait for services to be running or healthy and implies detached mode. It is a command-completion guarantee, not a traffic guarantee: the connection gap has already happened before it returns.
- (claim) ref=[D3r3b-4] status=verified class=ecosystem pub=2026-07 — docker-rollout v0.14 (2026-07-12) is alive, unarchived, MIT, roughly 3.3k stars with 4 open issues, but shipped only ONE release in the trailing 12 months. Documented limitation: services cannot have container_name and ports defined, so it works only behind a proxy and never with published host ports, and it requires real healthchecks. Both constraints are already satisfied by a Traefik architecture.
- (claim) ref=[D3r3b-5] status=verified class=version pub=2026-08 — Self-hosted PaaS field, none unmaintained, all released within 60 days: Coolify v4.3.3 (2026-08-15, 5 releases in 3.5 weeks, bundles Traefik, v4.3.0 explicitly added Traefik 3.7 support); Dokku v0.38.27 (2026-08-12, 7 releases in 5 weeks, documents 10s wait plus 60s drain); CapRover v1.15.2 (2026-08-15 but lumpy, dead Jun-Nov 2025 and Nov 2025-May 2026, nginx on Swarm, start-first only for apps without volumes); Kamal v2.12.0 (2026-06-18, about 8 releases in 12 months and decelerating toward quarterly, kamal-proxy advertises gapless deployments).
- (claim) ref=[D3r3b-6] status=verified class=pattern pub=2026-08 — The PaaS decision comes FIRST and makes the proxy choice downstream, because all four candidates own their own front door. Coolify preserves the Traefik v3.7 plus Cloudflare DNS-01 design; CapRover discards it for nginx on Swarm and additionally forces a Compose v3 format migration across all 12 apps since Docker documents stack deploy as incompatible with the current spec; Kamal discards it for kamal-proxy.
- (decision) D3 zero-downtime resolved in favour of the NULL option: docker-rollout plus self-managed Traefik delivers the same zero-downtime property on the already-designed stack. A self-hosted PaaS's real value here is UI, git-push deploys and backups - NOT zero-downtime, which is obtainable without it. Coolify's principal risk is upgrade cadence (5 releases in 3.5 weeks is a treadmill for a solo operator), not resource footprint. No overhead numbers are reported for any PaaS because none publishes measured footprints; sizing must be settled by measuring on the box.
- (decision) All four dimensions CLOSED. D1 closed on coverage (2 rounds), D2 closed on coverage (3 rounds), D3 closed on coverage (3 rounds), D4 closed after 1 round with four sub-items explicitly unevidenced. Proceeding to red-team pass then synthesis.
- (event) RED TEAM (layout) landed. Digest: digests/redteam-layout.md. Attacks accepted as valid.
- (decision) RED TEAM HIT ACCEPTED (fatal to justification): the 'no orchestrator covers Elixir/Solidity' finding decides whether a monorepo would be ACCELERATED, not whether it would be WORSE than 15 repos. Absence of a bonus is not presence of a cost, and D1 established that no measured CI fan-out numbers exist, so the one real monorepo cost is unevidenced. The orchestrator finding must be demoted from headline justification to a supporting note, and criterion 4 (incremental adoption) promoted to the actual headline reason.
- (decision) RED TEAM HIT ACCEPTED (serious): ranked criterion 2, portfolio value, was never scored. D1 named project liveness and audience navigation as the strongest pro-monorepo argument surviving at N=1 and 'close to the point' for a public portfolio, and the provisional verdict then picked the option that loses on it silently. Critically, Streamdal's symptoms were on GITHUB (stale timestamp, wrong language bar); an app registry page on cuatro.dev does not touch github.com/<user> and is an unevidenced substitute.
- (decision) RED TEAM HIT ACCEPTED (serious): Option 4's deferral was circular, not cautious. The gate - does a shared React package accumulate - is suppressed by the deferral itself, and the no-orchestrator finding is inapplicable to four Next.js apps where Turborepo genuinely works. Reclassified from under-argued to WRONG. Option 4 is decided NOW as a merge.
- (decision) RED TEAM HIT ACCEPTED (internal inconsistency): the provisional verdict named cuatro-portfolio as the hub and then specified building a DIFFERENT repo to be the hub. Resolved in the portfolio's favour. Also accepted: a tokens/YAML/JSON/Compose repo would reproduce the exact wrong-language-bar symptom in the new flagship, and 'two frozen files' was inaccurate because a SHA pin churns by definition.
- (decision) VERDICT REVISED in response to red team - the FIFTH OPTION the prompt invited: 'Anchored hub'. cuatro-portfolio becomes a Turborepo monorepo containing the four Next.js apps (portfolio, finance, tracker, cs-tournament) AND publishes tokens plus reusable workflows consumed by the remaining independent satellites (Elixir, Go, Python, Svelte, Vue, Angular, Solidity). This takes the estate 15->12 before archiving, makes the flagship repo genuinely alive because four apps' commits land in it, keeps Turborepo inside the JS/TS boundary where it actually works, requires no orchestrator for the polyglot satellites, and federates tokens to everyone. Combines options 3 and 4 rather than deferring one.
- (claim) ref=[RT-1] status=verified class=pattern pub=2026-08 — Red team confirmed the tokens-only design-system verdict survives attack: D2 is the best-evidenced section, with Material Web, Primer view_components, Adobe Spectrum and Carbon all converging independently. Also confirmed as failing: the repo-count-as-cost argument (repo count is a cost driver nowhere in the evidence; Grab's pain was commits and refs) and any deployment-based attack on the layout (D3's constraint is CPU and bots, fully orthogonal to source layout).
- (decision) NEW REQUIREMENT added mid-run by user: apps with login accounts must share one identity across the ecosystem (single sign-on). Added as dimension D5 - identity. Rationale for treating it as first-class rather than an implementation detail: a shared account is arguably a stronger 'suite' signal than visual coherence, and it interacts directly with three existing decisions - subdomain routing (Domain=.cuatro.dev cookie scope), database topology, and the 2 vCPU budget (a JVM identity provider such as Keycloak is a serious CPU tenant). Note the structural symmetry with D2: components cannot span six frameworks because they are a CODE dependency, but OIDC is a NETWORK PROTOCOL and therefore federates across all six stacks. Auth may be the only layer that genuinely unifies the estate.
- (assumption) Migration cost flagged for D5: cs-tournament currently uses Supabase for auth, so adopting a self-hosted or different identity provider is a migration with real user-record implications, not a greenfield choice. Other likely login-bearing apps by inventory: cuatro-finance, cuatro-tracker, cs-tracker (Phoenix), digital-library, StreamVault, possibly Mutuo. Exact set to be confirmed with the user before sequencing.
- (event) RED TEAM (topology + sustainability) landed. Digests: digests/redteam-topology.md, digests/redteam-sustainability.md. Majority of attacks accepted.
- (decision) RED TEAM HIT ACCEPTED (FATAL): the capacity arithmetic does not close and had no gate. 2 vCPU is a 200 pct CPU budget; the sole measured datapoint is one Next.js at 103.3 pct. Four Next.js apps demand 412 pct before BEAM, Postgres or Traefik. The two-source bar was met zero times for every other footprint, so UNKNOWN numbers were being treated as SMALL ones. Correction: the plan must state an explicit stop threshold, a measure-first step, and a named overflow target before any app is placed.
- (decision) RED TEAM HIT ACCEPTED (fatal, compounding): the portfolio app registry at sequencing step 3 is a CRAWLER AMPLIFIER - one page linking all 12 subdomains, when the measured CPU spike was caused by bots. The registry must not ship before the bot filter is active. Also accepted: docker-rollout DOUBLES peak resource use per deploy on an already-oversubscribed box, and its unpriced prerequisite is 12 real healthchecks across 6 frameworks.
- (decision) RED TEAM HIT ACCEPTED (fatal): next build was named the top unmeasured risk and then went unaddressed, while docker-rollout structurally presupposes prebuilt images. CORRECTION ADOPTED: build in CI and push to GHCR; the box only ever pulls images and never compiles. The hub already owns reusable workflows, so this costs nothing extra.
- (decision) RED TEAM HIT ACCEPTED (serious): Railway was dismissed on wrong arithmetic. 61 USD/mo is INSIDE the stated 40-100 USD/mo budget, not over it, and with the VPS sunk to 2028 the marginal spend is what matters. Offloading 2 heavy Next.js apps buys back a full box of CPU for 15-30 USD/mo on FOUR-SOURCE primary pricing, while bot filtering - which was ranked first - rests on ONE contested thread. The ordering was backwards on both evidence quality and effect size.
- (decision) RED TEAM HIT ACCEPTED (FATAL to the automerge policy): Renovate automerge-on-green-CI is VACUOUS where no CI exists, because 'no failing checks' degrades to merge rather than block. Compounded by this run's own semver finding that a colour VALUE change is a MINOR bump - routing the most visually breaking payload to unattended merge across 15 repos. Compounded again by D3's finding that traffic is bots not humans: no users means NO ERROR SIGNAL, so breakage is permanent-until-noticed-by-accident. CORRECTION: no automerge except where a real test suite exists; Renovate opens PRs and they are merged by hand.
- (decision) RED TEAM HIT ACCEPTED (serious): the npm plus Renovate propagation machinery is amortised over a token-change frequency that will not occur for a solo developer. CORRECTION: start by hand-copying one tokens.css into the apps that need it; the package, the registry-based propagation and the Renovate preset are EARNED later if token churn actually materialises. Build the mechanism when the frequency justifies it, not before.
- (decision) RED TEAM HIT ACCEPTED (serious): failing to archive is the single biggest sustainability risk and the 8-step plan contained ZERO deletions. 15 repos is 15 times N chores. The final plan must lead with archiving, and the per-project calls must be ruthless rather than diplomatic.
- (decision) RED TEAM HIT ACCEPTED (serious): with no users there is no error signal, so an EXTERNAL uptime and certificate-age check must be bought BEFORE any automation is added. The research named certificate-age alerting and PSI monitoring as mandatory while specifying zero mechanisms for either. Ranked by absence-of-signal, the chores that decay first are restore verification (no published guidance exists), then PSI (no published threshold), then Compose limit verification, then backup checking, then token semver rigour, then Renovate triage.
- (decision) RED TEAM HIT ACCEPTED (methodological, against this run): the two-source bar was applied ASYMMETRICALLY - proxy RAM figures were refused for resting on content farms while the headline bot-filter recommendation was built on one contested discussion thread. The final report must either demote the bot-filter claim to match its evidence or state the asymmetry openly. Also accepted: the provisional verdict laundered the research's hedges into flat assertions (Traefik/Cloudflare DNS-01 is medium-high confidence, PSI has no published threshold, restore-testing guidance does not exist).
- (decision) RED TEAM HIT ACCEPTED (framing): criterion 3 should be restated as 'reads as one author' rather than 'feels like one product'. The Primer evidence shows what Primer SHIPS, not what visitors PERCEIVE, and stack variety and suite coherence are in direct tension - which the research never stated despite the user explicitly valuing variety. Naming that tension honestly is better than implying tokens dissolve it.
- (source) D5a r1 batch: Keycloak sizing docs, authentik requirements, Pocket ID releases (v2.13.0 2026-08-07), ZITADEL requirements, WorkOS and Auth0 pricing pages (read 2026-08-15). Digest: digests/D5a-r1-1.md
- (claim) ref=[D5a-1] status=unverified class=performance pub=2026-08 — Keycloak appears DISQUALIFYING on its own sizing numbers for a 2 vCPU box: 1250 MB base RAM per pod at 10k cached sessions, 70 pct of the container limit to JVM heap plus about 300 MB non-heap, 1 vCPU per 15 password logins/sec, plus 150 pct extra headroom, with its own worked example requesting 3 vCPU per pod. SINGLE SOURCE, two-source class not met, and silent on whether a native build changes the picture.
- (claim) ref=[D5a-2] status=verified class=performance pub=2026-08 — authentik's vendor-stated minimum is at least 2 CPU cores and 2 GB of RAM, meaning it wants the ENTIRE box's CPU as its floor, and it is a multi-container stack additionally requiring Postgres. Disqualifying on this hardware.
- (claim) ref=[D5a-3] status=verified class=ecosystem pub=2026-08 — Pocket ID is the only option verified as both healthy and plausibly cheap: v2.13.0 (2026-08-07), nine releases since April, roughly monthly cadence, and its changelog shows a SQLite path so no dedicated Postgres is required. Its Go runtime and actual footprint remain UNVERIFIED.
- (claim) ref=[D5a-4] status=verified class=version pub=2026-08 — ZITADEL's requirements page states no CPU or RAM figures at all, but does require PostgreSQL 14-18 and a reverse proxy with HTTP/2 upstream (h2c/h2), with Traefik v3.x listed as tested. The h2c upstream requirement is a real configuration constraint for this deployment.
- (claim) ref=[D5a-5] status=verified class=cost pub=2026-08 — Read at vendor source: WorkOS is free for the first 1M MAU with SSO/SCIM at 125 USD per connection; Auth0 Free covers 25,000 MAU with B2C Essentials from 35 USD/mo and B2B from 150 USD/mo. NEITHER pricing page states an application or client cap - that is silence, not 'unlimited', and the question remains unresolved.
- (question) OPEN from D5a: Logto, Casdoor and Ory entirely unexamined. Clerk, Kinde, Stytch and Supabase pricing REFUSED because that search returned only listicle and content-farm domains - their circulating figures are logged as leads to verify at vendor source, not as findings. Supabase user-export question NOT answered: auth.users and auth.identities are confirmed queryable and CSV-exportable, but nothing authoritative was found on encrypted_password, hash format or cross-provider import. Do NOT plan a no-password-reset migration on current evidence.
- (event) NOTE the structural echo across dimensions: the two most popular self-hosted identity providers are disqualified by their own stated minimums on this hardware, exactly as the two best-resourced cross-framework component libraries were killed by their own vendors. In both cases the popular answer is the wrong one for a solo operator on a small box, and the evidence for that comes from the vendors themselves rather than from critics.
- (source) D5b r1 batch: MDN cookie __Host- prefix docs, RFC 9700 / BCP 240 (2025-01), Phoenix LiveView v1.2.9 security/auth docs, OpenID Foundation logout specs, Mozilla bug 1660574 + Microsoft Learn on third-party cookies, oidcc v3.8.0, oidc-client-ts 3.5.0, angular-auth-oidc-client 21.0.2. Digest: digests/D5b-r1-1.md
- (claim) ref=[D5b-1] status=verified class=pattern pub=2026-08 — VERDICT: OIDC Authorization Code plus PKCE, not a domain-scoped shared cookie. The decisive fact is MDN-confirmed: __Host- cookies must not specify a Domain attribute, so domain-scoped SSO permanently forfeits the strongest cookie hardening across all seven apps, and any subdomain can then set a cookie its siblings accept. The deeper problem is six languages sharing one session format; JWKS is the only lingua franca they have.
- (claim) ref=[D5b-2] status=verified class=pattern pub=2026-08 — Best architecture is HYBRID: OIDC federates between a central provider and each app, and each app then keeps its own ordinary host-only __Host- session. This gives language-neutral SSO with no cross-subdomain forgery surface.
- (claim) ref=[D5b-3] status=verified class=pattern pub=2026-08 — The LiveView WebSocket-versus-redirect problem DISSOLVES on primary docs (v1.2.9): LiveView begins its life-cycle as a regular HTTP request and shares authentication logic with regular requests through plugs. Run the OIDC callback in a plain Phoenix controller via oidcc_plug, write the session, and mount/3 receives it when the socket later connects; use router-level on_mount hooks with authed and unauthed routes in separate live_sessions.
- (claim) ref=[D5b-4] status=verified class=failure pub=2026-08 — Documented LiveView gotcha: an open WebSocket will NOT observe a logout unless the user reloads the page. The fix is to wire live_socket_id broadcast to force a disconnect. This is exactly the kind of silent failure that a no-users estate would never surface on its own.
- (claim) ref=[D5b-5] status=verified class=pattern pub=2025-01 — RFC 9700 / BCP 240 (January 2025) settles the flow question: the implicit flow SHOULD NOT be used, the password grant MUST NOT be used, and public clients MUST use PKCE. Authorization Code plus PKCE applies to SPAs and server-rendered apps alike.
- (claim) ref=[D5b-6] status=verified class=pattern pub=2026-08 — All four OIDC logout specifications are Final. Front-channel logout is structurally broken by third-party cookie blocking (Mozilla bug 1660574, Microsoft Learn) - though notably a single-apex topology like this one is same-site, the one case where it may still function. Recommend RP-Initiated plus Back-Channel logout.
- (claim) ref=[D5b-7] status=verified class=version pub=2026-08 — Client libraries: oidcc v3.8.0 is the Elixir standout under ErlEF Security WG governance and OpenID Certified, with oidcc_cowboy explicitly deprecated; oidc-client-ts 3.5.0 is the SPA common denominator across Svelte, Vue and React; angular-auth-oidc-client is at 21.0.2; Go's go-oidc v3 RemoteKeySet and Python's PyJWKClient both auto-refresh JWKS on an unknown kid, which is the key-rotation story.
- (claim) ref=[D5b-8] status=verified class=pattern pub=2026-08 — Consent screens are skippable for first-party clients across products (Ory require_consent false, Better Auth skipConsent, Duende RequireConsent false, Auth0 first-party flag), which is what makes a Google-Workspace-like no-prompt experience between one's own apps achievable.
- (question) OPEN from D5b: (1) the claim that Auth.js/NextAuth is in maintenance mode with users moved to Better Auth is CONTENT-FARM ONLY, low confidence, fails the two-source bar - must be verified at registry.npmjs.org/next-auth before acting. (2) No authoritative published guidance exists on cookie-versus-OIDC for first-party suites; the verdict is derived from spec mechanics rather than quoted from a source. (3) Traefik ForwardAuth as a THIRD architecture - centralised validation at the proxy, uniquely enabled by this deployment - was not researched at all.
- (source) D5a r2 batch: Pocket ID repo + docs (v2.13.0), Logto docs requirements, Casdoor releases (v3.153.0 2026-08-13), Ory Kratos releases, Clerk/Kinde/Stytch/Supabase pricing pages (read 2026-08-15), Better Auth Supabase migration guide. Digest: digests/D5a-r2-1.md
- (claim) ref=[D5a-6] status=verified class=pattern pub=2026-08 — DECISIVE: Pocket ID is effectively PASSKEY-ONLY. Its repository states it only supports passkey authentication so no password is needed; there is no password login, with only email one-time codes (EMAIL_ONE_TIME_ACCESS_AS_UNAUTHENTICATED_ENABLED) and LDAP as fallbacks. It is otherwise a genuine OpenID Connect Certified authorization server at v2.13.0 (2026-08-07), 1-3 week cadence, Go backend, SQLite default with Postgres optional, and no stated resource needs anywhere.
- (claim) ref=[D5a-7] status=verified class=cost pub=2026-08 — Supabase consolidation does NOT force a password reset in general: Supabase Auth uses bcrypt, auth.users.encrypted_password is exportable, and Better Auth's official migration guide maps it straight across with bcrypt.compare() verification. HOWEVER Pocket ID, having no password login at all, would force passkey enrollment for every existing user regardless. The migration cost is provider-dependent, not universal.
- (claim) ref=[D5a-8] status=verified class=performance pub=2026-08 — Logto is likely disqualified on the vendor's own numbers: docs.logto.io states a minimum recommended 2 vCPU / 8 GiB / 256 GiB plus PostgreSQL ^14, which is the entire box. Vendor-stated, not measured.
- (claim) ref=[D5a-9] status=verified class=cost pub=2026-08 — Managed pricing read at vendor pages 2026-08-15: Clerk explicitly states 'Every plan supports unlimited applications', free to 50,000 MRU per app, Pro 25 USD/mo. Kinde says applications are unlimited but METERS ENVIRONMENTS (1 prod + 1 non-prod on Pro, 5 USD each extra), free to 10,500 MAU. Stytch is free to 10,000 MAU and is SILENT on any application cap - reported as silence, not unlimited. Supabase Free caps at 2 active projects and 50,000 MAU. Combined with WorkOS free to 1M MAU, the application-cap risk that would have disqualified managed options does not materialise.
- (decision) D5 VERDICT FORMING - managed identity, not self-hosted, and the reasoning is the red team's CPU finding rather than a preference. The self-hosted field is nearly empty on this hardware: Keycloak wants 3 vCPU in its own worked example, authentik's floor is 2 cores plus Postgres, Logto's stated minimum is the entire box, and the one cheap survivor (Pocket ID) is passkey-only. Meanwhile Clerk is free to 50,000 MRU with explicitly unlimited applications and WorkOS is free to 1M MAU. Identity is the single component that can be offloaded for zero dollars AND zero CPU on a box whose binding constraint is CPU - and it is security-critical, so not owning the patching is a sustainability win on ranked criterion 1. Pocket ID remains the runner-up IF self-hosting is wanted for portfolio-story reasons, with passkey-only as the stated price.
- (question) OPEN from D5a r2, honest failures: a Logto version/date conflict was NOT resolved (GitHub releases returned 2024 dates, search returned 2026; the researcher correctly refused to average). Ory Hydra - the component that actually matters for OIDC in the Ory stack - was never reached before budget ran out. Casdoor is extremely alive (ten tags in four days) but its database and runtime requirements went unestablished, and that release velocity is itself a churn signal worth weighing for a solo operator.
- (decision) USER DECISIONS 2026-08-15: (1) Identity = MANAGED (Clerk or WorkOS), self-hosting declined. (2) Apps needing logins confirmed as NINE plus one: cs-tournament, cuatro-finance, cuatro-tracker, cs-tracker, digital-library, StreamVault, MaiCoin, poketracker-go, tcg-tracker, with Mutuo needing demo accounts only. This is materially more than the 7 assumed, which strengthens the OIDC case and makes the application-cap question decisive.
- (decision) IDENTITY PROVIDER PICK: Clerk over WorkOS, on the application-cap evidence. With ten login-bearing clients, Clerk's explicit vendor statement that 'every plan supports unlimited applications' (free to 50,000 MRU per app, Pro 25 USD/mo) beats WorkOS's SILENCE on any application cap. WorkOS's 1M MAU free tier is more generous on volume, but volume is not the constraint here and silence is not a guarantee. Note MaiCoin is Solidity/Web3 where wallet-based auth is the norm rather than OIDC, and tcg-tracker is an empty shell - both qualify the count.
- (source) D5c batch: Traefik v3.7 ForwardAuth reference, oauth2-proxy releases (v7.15.3 2026-06-09), nextauthjs README + authjs.dev, npm registry next-auth dist-tags, Better Auth releases (v1.6.29 2026-08-14). Digest: digests/D5c-r1-1.md
- (claim) ref=[D5c-1] status=verified class=pattern pub=2026-08 — Traefik v3.7 ForwardAuth delegates EVERY request to an auth service (2XX allows, non-2XX returns the auth service's own response) and can inject identity upstream via authResponseHeaders, authResponseHeadersRegex and headerField, so it CAN replace per-app OIDC for identity delivery. Documented limitations: no caching is documented, meaning a per-request round trip on a 2 vCPU box; forwardBody true 'breaks streaming' verbatim; maxBodySize and maxResponseBodySize default unbounded with an explicit DoS warning.
- (claim) ref=[D5c-2] status=unverified class=failure pub=2026-08 — CRITICAL GAP: WebSockets are not mentioned anywhere in the Traefik v3.7 ForwardAuth reference. The reading that a LiveView socket is authorized once at handshake and never re-checked is INFERENCE, not citation, and must be tested before being relied on. Separately, if an app trusts a proxy-injected identity header, anyone reaching that app NOT through Traefik becomes any user - there is no cryptographic binding and network position is the only control, which on one VPS running ten apps is the default condition. Flagged as reasoning, one source short of the security bar.
- (decision) FORWARDAUTH REJECTED as the primary architecture, on three grounds: an undocumented-caching per-request round trip is the wrong tax on a box whose binding constraint is CPU; WebSocket behaviour is entirely undocumented and one app is LiveView; and header-trust has no cryptographic binding on a host where apps are reachable directly. It IS retained for a narrow, genuinely good use: gating things that have no auth of their own - the Traefik dashboard, admin surfaces, and any static demo that should not be public.
- (claim) ref=[D5c-3] status=verified class=ecosystem pub=2026-08 — CONFIRMED at two primary sources with a material correction: the nextauthjs README states verbatim 'Auth js is now part of Better Auth. We recommend new projects to start with Better Auth', corroborated by authjs.dev. But 'maintenance mode' is the WRONG word and appears in neither primary source - this is a merger and stewardship change, and next-auth 4.24.15 plus 5.0.0-beta.32 shipped 2026-07-20 with security fixes including auth failing open. v5 remains beta (npm latest is 4.24.15).
- (claim) ref=[D5c-4] status=verified class=version pub=2026-08 — Better Auth v1.6.29 (2026-08-14), near-weekly cadence, v1.7.0 in RC with breaking OAuth changes. It documents BOTH roles but asymmetrically: usable as an OIDC CLIENT via the SSO plugin, while its OIDC PROVIDER plugin self-warns it 'may not be suitable for production use' and is slated for deprecation. Relevant here as a client only, never as the central identity provider.
- (claim) ref=[D5c-5] status=verified class=version pub=2026-06 — oauth2-proxy v7.15.3 (2026-06-09) is a CVE-remediation release, so the project is not stalled; only two releases were retrieved so 12-month cadence is unestablished.
- (event) SEMANTIC CITATION CHECK: 13 claims across 12 sources, 0 mismatches, every quoted string verified verbatim. THREE corrections applied to the report. Digest: digests/citation-check.md. Scope limit recorded openly: roughly 85 pct of the 97 sources were not re-checked.
- (claim) ref=[D5a-1] status=overturned class=performance pub=2026-08 — CORRECTED: Keycloak publishes NO minimum. The 3 vCPU figure is one pod of a THREE-POD CLUSTER sized for 45 logins/sec, i.e. a capacity-planning example, not a floor. Confidence downgraded high to medium; Keycloak reclassified as poor fit rather than disqualified; the cross-dimension claim that Keycloak, authentik and Logto all publish minimums exceeding the box is FALSE for Keycloak and has been narrowed to authentik and Logto.
- (claim) ref=[D3b-1] status=disputed class=performance pub=2026-02 — CORRECTED FRAMING: the 639,880 KB / 103.3 pct figure is exact and correctly transcribed, but was measured DURING A BOT CRAWL. The four-apps-equals-412-percent projection therefore treats an attack peak as steady state and is an upper bound on a bad day, not expected demand. Confidence downgraded to medium-low. The Step 1 measurement gate is unchanged and remains the correct remedy.
- (claim) ref=[D3b-5] status=overturned class=cost pub=2026-08 — ARITHMETIC ERROR CORRECTED: Railway unit prices verified correct, but 12 apps times 6.40 USD equals 76.80 USD, not the 61 USD/mo previously stated. Corrected to approximately 77 USD/mo memory-only throughout, which is near the top of rather than comfortably inside the 40-100 USD budget.
- (event) EDITORIAL REVIEW (structure + prose lenses per doc_standards). Digest: digests/editorial-review.md. Highest-impact fixes applied: the 15-to-11 headline was self-contradictory and is corrected to 15-to-8 with the intermediate 15-to-12 checkpoint named; sub-question 4 was analysed but never decided and now carries an explicit 'do not adopt devcontainers' call; a sub-question-to-answer map was added; D3's bot-filter headline was demoted in place to match the Contrary-evidence correction rather than being contradicted 500 lines later; the unsourced 'overwhelming majority of perceived brand coherence' quantifier was removed; anchor/satellite/app-registry terminology was defined in the executive summary; cross-dimension insight 6 was cut to a pointer; D4 carries a skip-on-first-read signpost.
- (event) RUN COMPLETE. Five dimensions closed, red team of three run and largely accepted, semantic citation check run with three corrections applied, editorial review run and applied. research.md status set to complete. Refresh due after 2026-11-15; 12 of 40 tracked claims already past their re-check window, chiefly version numbers on a one-month bar.
