---
epic: 2
date: 2026-09-25
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 2 Retrospective

The suite becomes visible, Registry as product and the reshaped front door.

## Epic summary

**Mode:** sprint. **Epic:** 2, supplied on the invocation (`-H epic 2`), run headless on the
Operator ruling 2026-09-24 that places this retrospective after the day's fixes and before the Epic 2
merge to `main`.

**Completeness:** `sprint_status.py detect-epic --epic 2` reads `story_count: 31` and
`pending_stories: []`. All 31 stories are `done` (`sprint-status.yaml:166-487`), and the retro key read
`optional` before this run.

**Diff range:** `681a4c6^..82fb667`, 2026-08-29 to 2026-09-25. `681a4c6` is the first Epic 2 story
commit (`fix(2-1)`); its parent `f5df8c8` is the Epic 1 retrospective's own commit, so nothing of Epic 1
is inside the range except the two Epic 1 stories the Operator closed on 2026-09-24 (below). `82fb667`
is the head of `dev`, level with `origin/dev`.

The range has two parts, and the ruling puts both inside this retrospective:

- **The 31 stories**, `681a4c6` to `9bf4f20` (2026-09-23), which closed the epic on the board.
- **The Operator-ruled fix packages**, `9bf4f20..82fb667` (2026-09-24 and 2026-09-25), 66 commits,
  every one scoped to its package: `dw-36`, `dw-113`, `dw-43`, `dw-121`, `registry`, `dw-15`, the
  Epic 1 close (`1-19`), `dw-94`, `dw-90`, `retro-3`, `records`, `process` and `vercel`. Two earlier
  packages, `a-17` (2026-09-15) and `dw-115` (2026-09-23), sit inside the first part.

**Volume** (`git_evidence.py` over the range): 213 commits, 5 merges (all measured), 344 paths,
88,100 lines added and 11,730 deleted (net +76,370). 49 binary revisions are unmeasured churn: the
retired font binaries, the deleted 3D assets and logo raster, and five regenerations of the `/work`
baseline. Lines added by directory: `_bmad-output/` 25,458, `ops/` 24,377, `tests/` 22,233,
`components/` 10,135, `app/` 2,305, `lib/` 1,370, `hooks/` 701, `contracts/` 353.

**Attribution:** every one of the 31 stories has at least one commit naming it (2-26 has 17, 2-25
has 11). All 42 `feat:` subjects carry a story or package key; the 26 subjects with a type and no
scope are documentation and tooling chores and one fix (`415e05b`, DW-30's), most of them naming a
story in their text.

**Where it runs:** `main` is at `1e93787` (PR #75, 2026-09-13), so production carries Epic 2 through
Story 2-25. `dev` is 134 commits ahead: the redesign (2-26 to 2-34 and 2-22) and every fix package are
not live.

### Evidence inventory

| Evidence | Status | Source |
|---|---|---|
| Epic spec with declared criteria | available | `_bmad-output/planning-artifacts/epics.md:838-861` (the epic), `:1998-3843` (the stories), `:3120-3132` (the four criteria binding every redesign story) |
| Epic context digest | available | `_bmad-output/implementation-artifacts/epic-2-context.md` |
| Story specs | available, 31 of 31, all `status: 'done'` | `_bmad-output/implementation-artifacts/spec-2-*.md` |
| Fix package specs | available, 15, all `status: 'done'` | `spec-a-17-*`, `spec-dw-*`, `spec-epic-1-close`, `spec-process-and-upkeep`, `spec-records-and-docs`, `spec-registry-1-2-0`, `spec-retro-3-cold-reviews`, `spec-vercel-removal` |
| Diff range and per-story commits | available | `git_evidence.py` over `681a4c6^..82fb667` |
| Sprint status | available | `_bmad-output/implementation-artifacts/sprint-status.yaml` |
| Deferred-work ledger | available, 245 entries, 104 open | `_bmad-output/implementation-artifacts/deferred-work.md` |
| Operational records | available, 28 | `ops/*.md` |
| Previous retrospective | available | `_bmad-output/implementation-artifacts/epic-1-retro-2026-08-29.md`, and its 14 items in `sprint-status.yaml` `action_items` |
| Architecture and PRD | available | `ARCHITECTURE-SPINE.md`, `prds/.../prd.md`, `DESIGN.md`, `EXPERIENCE.md`, `RESTYLE-SPEC.md` |
| Session logs | available, not read in full | 41 Claude Code transcripts last written between 2026-08-29 and 2026-09-25 in the project's session store, 86.5 MB; no `bmad-loop` run belongs to this epic (`.bmad-loop/runs/` ends 2026-08-27) |

Nothing on the checklist is missing. Two inputs were narrowed, and the narrowing is stated in place.

## Findings

**Scope note.** No sub-agent tool was available to this run, so every aggregate view below was derived
inline, by scripts where a number is involved. `bmad-review` was invoked with its adversarial,
edge-case and verification-gap lenses on a **narrowed scope**: the final state of the twelve Hub
source files that the most separate sessions edited (group E), because every story and package
already ran its own review layers, and the seams between sessions are what this retrospective alone
can see. The session transcripts (86.5 MB) were not read in full: the board's notes, the specs'
change logs and the ledger carry every process fact in group F, and one targeted search of the
transcripts is named where it was used. Every finding carries its source, and every ledger claim
routed here was re-read in the ledger first.

### A. Spec-to-implementation reconciliation

**A1. The front door states something the Registry no longer supports.** *Severity: high for the
merge. Disposition: fix now, before the merge.*

`components/organisms/Premise/Premise.tsx:68-70` sets `EXPERIENCE.md`'s premise verbatim: "Everything
below is running right now". Since `fa34f09` (Registry 1.4.0, the Vercel removal on Operator ruling
2026-09-24) one of the six rows below it, `cs-tournament`, is `Complete` and runs nowhere, and the
Directory's own count reads `5 running` (`SuiteDirectory.tsx:189-191`). The Registry is truthful; the
page is not. The epic's promise is that "the Registry cannot lie about either of them"
(`epics.md:842`), and the copy now does what the data does not.

The same page states three counts on three denominators within one scroll: "Fourteen personal
projects" (every entry, `Premise.tsx:96`), `5 running` (`Live` rows) and "Six applications" in the
footer (rendered rows, `SiteFooter.tsx:65-74`), whose "five languages" are estate-wide.

No test ties the sentence to the statuses (group E, verification gap), so every suite stayed green
and the package found it by reading. Filed as **DW-244**, owner the Operator, trigger "before the Epic
2 merge to `main`". The wording is the Operator's, because `EXPERIENCE.md` fixes the copy word for
word.

**A2. The Registry claims a `token_contract` that the application it describes does not serve yet.**
*Disposition: fix now, by sequencing the merge.*

`contracts/registry.json` on `dev` declares `cs-tracker` at `2.0.0` (`ae7ffb2`, Operator ruling
2026-09-24). `cs-tracker`'s re-vendor is the local, unpushed commit `991d0f6`, and its remote `main`
and the deployed application read `Contract v1.0.0`. The Registry verification (FR-32, Story 2-23)
says exactly that: all four of its runs since `39ef7bc`, each on a `dev` push touching the Registry,
failed on the same one check, the latest **36102518256** on `82fb667` at 33 of 34:
`cs-tracker token_contract: mismatch: the Registry declares 2.0.0 ... reads Contract v1.0.0`. The
record is honest about it (`ops/contract-adoption.md:808`, Pending Operator action 8 at `:825`, whose
step 1 also moves the Elixir suite's `v1.0.0` pin).

The mechanism is working; the consequence is for the merge. Merged before action 8, the published
Registry carries the claim to production, the job fails on the merge push (it runs on any change to
`contracts/registry.json`) and then daily on `main`, and the Operator ruling 2026-09-24 to "Merge Epic 2
to main when every check is green" cannot be met while the job is red.

**A3. FR-10 holds on five of the six rendered rows.** *Disposition: accept as ruled, and route the
remedy.*

FR-10 asks for a `source` "present and functional on every entry" (`epics.md:115-117`). `cs-tracker`
is `Live` and rendered, and its Source link answers 404 to an anonymous reader (run 36102518256:
"tolerated by KV-2"). KV-2 retired on the Operator ruling 2026-09-24 with `cs-tracker` and `Mutuo`
kept private by decision after a secret scan (`ops/known-violations.md:289-299`). So the one reader
FR-10 exists for, Marcus, meets a dead end on the one rendered row, and SM-3's `source-open` counts
those clicks as opens. The record says publishing "waits on the Operator's own review of its
findings". That review, and the revocation `ops/known-violations.md:297-298` asks for on `Mutuo`'s side
(a Twilio API key committed 2021-04-16, to be revoked "if it is still live"), exist only as prose: no
pending-action row carries either, and neither is on the ruling's list of the Operator's physical
actions. The revocation is a credential question and belongs in a tracked row.

**A4. UX-DR49's 140 KB non-3D budget is breached on every route measured, and nothing owns the
breach.** *Disposition: spec reconciliation, the Operator's.*

`ops/asset-budget.md:484-499` (2026-09-24, after the DW-15 package, the latest reading) puts on the
wire, gzipped, 231,829 for `/work`, 230,420 for `/cv`, 197,305 for `/celeste` and 197,189 for
`/_not-found`, the document a 404 renders, against 140,000. `/` is rendered on request and was not
fetched. The budget's own decomposition is inside, 104,112 with 35,888 of margin, because
`EXPERIENCE.md:944` defines the non-3D payload as HTML, one CSS file and three woff2 subsets, with no
line for the framework's JavaScript, 221,376 of it on `/work` (`ops/asset-budget.md:1816-1820`). The
epic asked every redesign story to **measure** against the budget (`epics.md:3126-3129`), and every one
did. Whether the Hub can ever meet UX-DR49 as written is a different question, and no ledger entry,
known violation or pending action carries it.

**A5. The epic's own header still describes the epic before the restyle change.** *Disposition: fix
now (documentation).*

`epics.md:2005-2010` reads "Twenty-six stories", "nineteen FRs", and places the five migration steps in
Stories 2.18 to 2.22. `epics.md:856` says the epic went "from 26 stories to 31", the FR list at
`:845-846` names twenty (FR-37 was added on 2026-08-15), and `:3091` says the redesign replaced steps 3,
4, 6 and 7. `git blame` dates the six lines 2026-08-15, one of them 2026-08-16, so the restyle change
of 2026-08-15 never reached them. It is the class the Epic 1 retrospective found at `epics.md:967`.

**A6. FR-1 still names a retired route, and Story 3.2 will inherit it.** *Disposition: tracked, as
**DW-138**.* FR-1 lists `/recommendation` among the routes that "remain reachable and functional"
(`epics.md:61-63`), and Story 3.2's criterion requires it to render, though Story 2-17 retired it on
the Operator ruling of 2026-09-11 and production answers 404 (observed 2026-09-25). DW-138 names the
owners (the Operator for FR-1, Story 3.2's planning for its criterion).

**A7. Deviations taken by Operator ruling, recorded so a later retrospective does not re-flag them.**
*Disposition: accept.*

- Contract **2.0.0** was published inside Epic 2 (`40591cd`, DW-15), though `epic-2-context.md:95-96`
  says Epic 2 does not move the contract.
- The Directory shows five `Live` rows and one `Complete`, where the epic's story reads "six running
  applications" (`epics.md:840`): `cs-tournament` left Vercel, and FR-28's rule (status and `live` move
  in one change) was exercised for real in `fa34f09`.
- `/recommendation` is retired (Story 2-17). The home readout panel (DW-110), drag-to-rotate (DW-119),
  Lenis and the root providers (DW-36) and `--hero-height` (DW-122) are removed.
- Story 2-8's two departures, KV-4 and KV-5, are both retired: KV-4 by Story 2-32 and KV-5 by
  Story 2-33, and the hit-target ledger is empty.

### B. Architecture delta

**B1. Checked and clean.** *Disposition: accept.* No dependency-graph tool is configured, so the graph
was derived from every import in the 40 product source files (`components/`, `app/`, `lib/`, `hooks/`,
`content/`, tests excluded): 57 local edges, **no cycle**, and **no lower layer importing a higher one**,
in the order `lib` and `content`, then `hooks`, `atoms`, `molecules`, `organisms` and `app`. `lib/registry.ts` is the
only product module importing from `contracts/`, and only `registry.json`, which
`app/__tests__/anchor-contract.test.ts` enforces. Across 213 commits **no runtime dependency was
added**; `lenis` and `three-stdlib` were removed (`package.json`). `contracts/` gained exactly the two
hand-authored files AD-4 allows, and nothing executable.

**B2. The deploy path changed shape, and the merge is its first production run.** *Disposition:
accept, watch at the merge.* Since `b0aeaff` (DW-94) the box runs `ops/deploy-remote.sh` read out of
the commit being deployed (`.github/workflows/deploy.yml`). The bootstrap, a checkout with no script
yet, is exercised against a scratch repository by `ops/__tests__/deploy-remote.test.ts`, and the two
proofs that need `main` are `ops/contract-serving.md` Pending Operator actions 7 and 8. Nothing has run
it on the box: `main` has not been pushed since 2026-09-13.

**B3. CI grew from five blocking jobs to seven.** *Disposition: record.* `registry-schema` (Story 2-3)
and `literal-conformance` (Story 2-34) joined, beside Lighthouse and the scheduled Registry
verification. On `82fb667` (run 36102518282) all seven are green, and `rendered-output` took 386 s,
where Epic 1's first CI run of the harness took 69 s for the whole job (run 32801557172,
`sprint-status.yaml:114-116`).

### C. Duplication map

**C1. The e2e suite shares five helpers and copies the rest.** *Disposition: defer to one test-only
story, with the identical copies first.*

`tests/e2e/harness.ts` exports five helpers (`:11`, `:68`, `:128`, `:182`, `:197`). Every other helper
lives in the spec that needed it, measured by extracting each definition and comparing it with
comments and whitespace stripped:

| Helper | Spec files defining it | Distinct bodies |
|---|---|---|
| `goTo` | 18 of the 24 | 10, seven files byte-identical |
| `plantStyle` | 6 | 3, four identical |
| `probeComputed` | 5 | **1**, all five identical |
| `settle` | 4 | 4 |
| the `FINGERPRINTS` parse of `ops/asset-budget.mjs` | 3 (`front-door`, `narrative`, `work-hero`) | not compared |

The ledger watched it grow and nothing acted: **DW-22** (Story 2-1, two navigation guards), **DW-232**
(Story 2-10, three copies of `goTo` and `plantStyle`), **DW-49** (Story 2-13, the third
`FINGERPRINTS` copy), all open. It went from two copies to eighteen.

**C2. Epic 1's probe duplication is unchanged and larger.** *Disposition: carried by Epic 1 action
10.* `ops/cs-tracker-adoption-probe.mjs` (now 2,066 lines, 1,803 at the Epic 1 retrospective) and
`ops/daisyui-route-probe.mjs` (1,465, was 1,341) still each define `moduleDir`, `say`, `run`,
`describeRun`, `readOrNull`, `serve`, `sameFile` and `thisFile`, and `ops/cs-tracker-accessibility-probe.mjs`
carries a third partial copy (`moduleDir`, `say`, `thisFile`).

### D. Size growth

**D1. The Hub's tests outweigh the Hub more than five to one, and the largest specs are helper
libraries.**
*Disposition: record; the fix is C1's.* The shipping source under `components/`, `app/`, `lib/`,
`hooks/` and `content/` is 5,595 lines. Its unit tests are 9,228 lines and the e2e specs 22,272;
`ops/` holds 13,431 lines of scripts, 14,146 of their tests and 20,776 of records across 28 files. Five
spec files pass 1,400 lines: `accessibility-floor.pw.ts` 2,296 (17 cases, 33 top-level helpers),
`front-door.pw.ts` 1,954, `narrative.pw.ts` 1,799, `hit-target-floor.pw.ts` 1,767 (17 cases) and
`chrome-nav.pw.ts` 1,418. Read, they are single-purpose suites whose bulk is local helpers, which is C1
seen from the other side, not god-classes. Comment density runs 20 to 29 percent.

**D2. The deferred-work ledger is the largest file in the repository and refills as fast as it
drains.** *Disposition: record, and see F5.* `deferred-work.md` is 7,698 lines (+6,369 in the range):
245 entries, DW-1 to DW-245 with none missing or repeated, 104 open. It held 74 at the Epic 1
retrospective. The packages of 2026-09-24 and 2026-09-25 closed 88 entries with dated notes and filed
24 new ones (DW-123 to DW-140, DW-240 to DW-245), 23 of them still open and 17 owned by the Operator.

**D3. `ops/asset-budget.md` is 2,358 lines**, one full reading appended per story and per package
(nineteen under § Every route, five on 2026-09-24 alone). *Disposition: record.* Append-only is the
record's design; **DW-108** notes that no figure in it is pinned by a test.

### E. Cross-story boundary review (`bmad-review`, narrowed scope)

Content: the final state at `82fb667` of `app/app.scss` (11 sessions), `HomeLayout.scss` (8),
`SuiteDirectory.tsx`, `Error404.tsx`, `SuiteDirectory.scss` and `HomeLayout.tsx` (6 each),
`lib/registry.ts`, `SkipControl.scss`, `hooks/useReduceMotion.ts` and `SiteFooter.scss` (5 each), plus
`Premise.tsx` and `SuiteReach.tsx`, which read the same Registry. Lenses run inline in sequence.

**E1. Verification gap, the premise.** *Disposition: fix now, with A1.* `Premise.test.tsx:62-100`
asserts the derived count, the noun's agreement, at most three sentences and no framework name, and
`tests/e2e/premise.pw.ts` reads geometry and status codes. Nothing reads the rendered entries'
statuses against the sentence, so `fa34f09` made it false with every suite green. The missing case: one
that fails when a rendered entry is not `Live` while the copy says running, written against whatever
wording the Operator rules. The block's docblock (`Premise.tsx:19-23`) also says nothing on it "states
a fact of its own" and none of it "can go stale"; the running sentence is such a fact and did.

**E2. Comments that restate counts and line numbers drift, one session after another.**
*Disposition: fix now (three comments, four citations), and a process lesson (F7).*

- Four files cite `lib/__tests__/registry.test.ts:621-668` as the refusal of a client component that
  value-imports the Registry (`Premise.tsx:14`, `SuiteDirectory.tsx:15`, `SiteFooter.tsx:20`,
  `CvIntro.tsx:18`). That `describe` now starts at `:765`; `:621-668` hold the footer count and family
  cases. The same citation was already corrected once, from `:189-235` (`spec-2-11`, line 232).
- `SuiteDirectory.scss:9-10` says the Hub's own custom properties live in `app/app.scss`, "one since
  Story 2-22". There are none since `2cbbccf` (DW-122).
- `hooks/useReduceMotion.ts:17-19` says the hook "has four consumers", `HomeLayout.tsx` first.
  `HomeLayout.tsx` has not called it since Story 2-29, as its own docblock says (`:43-46`); the three
  are `WorkItem.tsx:39`, `WorkHero.tsx:42` and `hooks/useNarrativePath.ts:175`.

The ledger holds more of the class: DW-54, DW-59 (derived sentences in `ops/hit-target-floor.md`),
DW-105 (e2e line citations), DW-211 (records citing a deleted file by line).

**E3. Two consequences of ruled fixes ship with the merge, both filed and both the Operator's.**
*Disposition: Operator decisions; neither blocks the merge unless the Operator says so.*

- **DW-127**: the landmark's inset focus ring (`app/app.scss:44-55`, Operator ruling 2026-09-24, F-20)
  is painted beneath the hero's canvas and scrim on `/`'s default door at 768 and wider, so after Enter
  on the skip link a keyboard visitor sees no ring in the first viewport there.
- **DW-125**: the ruled `visibility: hidden` link keyframe (`HomeLayout.scss:58-70`, DW-106) moved
  Lighthouse's mobile largest contentful paint on `/` from 2.4 to 2.7 s to 5.3 to 5.5 s, and the
  ungated performance score from 0.65 to 0.67 down to 0.49 (`ops/hub-accessibility-pass.md`, the
  2026-09-24 re-read). Performance is commented out of `.lighthouserc.js`, so no gate sees it.

**E4. The 404 numeral is the DW-113 class, left as text.** *Disposition: accept as-is, recorded.*
`Error404.tsx:49-51` renders `404` in `--token-accent-muted` (`Error404.scss:41`), `aria-hidden`, the
treatment the Operator ruling 2026-09-24 moved to generated content on `/` and `/work`. Lighthouse
refuses to audit a page answering 404 (`ops/hub-accessibility-pass.md`, § Stated limits), so no gate
scores it, and WCAG exempts pure decoration. Recorded so the divergence is a known one.

**E5. Edge-case lens: clean.** The paths the twelve files leave unguarded are closed upstream:
`contracts/registry.schema.json` refuses an empty or non-https `source` and `live` (`:76-79`,
`:102-105`), a family that is not kebab-case (`:107-110`) and an empty Registry (`:22`); `hubEntry`
refuses zero or several matches; `SuiteReach` guards a missing observer, storage, tracker and heading.

### F. Process

**F1. Six stories shipped in one day with reviews that were not independent, and the independent
verifier earned its place.** *Disposition: record; lesson in the action items.* 2-30, 2-31, 2-32,
2-33, 2-34 and 2-22 were built and pushed on 2026-09-23, each with its six review layers run inline
and recorded as not independent (`sprint-status.yaml:209`, `:359`, `:392-393`, `:427-428`,
`:459-460`, `:485`). The independent verifier then refused 2-22 on an ungated hover the header edit
had touched (`spec-2-22`, Spec Change Log, fix round 1) and caught a one-in-24 flake in 2-33's chrome
hover case (`spec-2-33`, fix round 1); it then refused the `dw-43` package twice on 2026-09-24 and
the `records` package twice on 2026-09-25 (their Spec Change Logs). Each refusal named something the
story's or package's own review had let through.

**F2. Assertions that could not fail kept appearing, and each was found by a later review.**
*Disposition: record; lesson in the action items.* Story 2-1's review found a vacuity check that
could not diverge and a `toHaveBeenCalled()` on a mock nothing cleared (`spec-2-1`, lines 141-144);
Story 2-29's audit and review found three (a collapse bound widened to what `heightSteps` satisfies by
construction, a reduced-motion poll the CSS satisfies anyway, an opacity regex that matched nothing:
`27a27cd`, `53df9c0`); Stories 2-30 and 2-31 each deleted a constant assertion (`spec-2-30`, lines
386-387; `sprint-status.yaml:393`); and `retro-3` made three Epic 1 contract-serving checks able to
fail (`abf3114`). The epic's own rule is that each new assertion is
"demonstrated failing against a deliberate fixture" (`epic-2-context.md:75-78`). Where a plant was
made it held; these escapes were assertions no plant exercised.

**F3. The board moved stories to `done` ahead of their own done condition.** *Disposition: fix now
for 2-27, and a lesson.* `spec-2-27`'s manual check (lines 353-357) and
`ops/hub-accessibility-pass.md` Pending Operator action 4 both say "the board moves to `done` after"
the screen-reader read of `/`. The board moved 2-27 to `done` on 2026-09-14 (`1addf8c`), and action 4
still reads `_not done_`. The board notes of 2-29, 2-30, 2-31, 2-32 and 2-33 each end "Waits on the
Operator" (`sprint-status.yaml:335`, `:362`, `:401`, `:435`, `:468`) under a `done` status; the ruling
of 2026-09-24 moved those checks onto the Operator's physical list. The board defines
`awaiting-operator` for exactly this state (`sprint-status.yaml:13-15`) and Epic 1 used it; Epic 2 did
not. One targeted search of the transcripts found no Operator message ordering the 2-27 move, so it
reads as an unrecorded override rather than a recorded one.

**F4. The oversized warning fired on 6 of 31 stories**, 2-22 and 2-30 to 2-34 (their frontmatter),
against 15 of 21 in Epic 1, and none was split. *Disposition: record.* The rule that halts Build on it
for the Operator's Split or Keep landed on 2026-09-24 (`0c88b5b`), after all six.

**F5. The Operator's decision queue refills from the work that drains it.** *Disposition: record;
lesson in the action items.* The redesign stories of 2026-09-23 filed seven decisions for the Operator
(DW-110, DW-113, DW-114, DW-117, DW-119, DW-121, DW-122), ruled together on 2026-09-24. The packages
that executed those rulings filed 17 more for the Operator in two days (DW-123 to DW-127, DW-129,
DW-132, DW-134, DW-136 to DW-138, DW-140, DW-240 to DW-242, DW-244, DW-245). Of the 104 open entries,
20 name the Operator as owner and 15 are unassigned. Only DW-244 and DW-172 say they bind the merge;
the rest do not say either way.

**F6. What held.** *Disposition: accept.* Attribution held: every story and package is found by its
key in the log, and the subjects that carry none are chores and one fix (Epic 1 action 13). Every story and
package left dated records with their evidence, which is why this retrospective could be written from
the repository. The Epic 1 action items went from 0 of 14 done to 12 of 14 inside this range.

**F7. Comments carry line numbers and counts that sessions then have to keep true.** *Disposition:
lesson in the action items.* E2 and the four ledger entries it cites are one pattern: a claim in a
comment that no test holds goes stale the first time an unrelated session moves the code under it.

## Behavior verification

Exercised on 2026-09-25 against the working tree at `82fb667`.

**Unit suite: green.** `corepack pnpm test --run` (Vitest 4.1.2): **1,634 tests in 63 files, all
passing, 98.11 s**, and the same 1,634 again in 83.40 s after this retrospective's board update,
since three ops suites read `sprint-status.yaml`. `AGENTS.md:55` states the same count.

**Browser suite: green, in the pinned image.** The rendered-output harness in
`mcr.microsoft.com/playwright:v1.62.1-noble` (`pnpm test:e2e`, the command `AGENTS.md:72` gives):
**337 passed in 7.9 min, exit 0**. That covers the front door on both doors (51 cases), the narrative
(30), the premise (24), the Plate mark and work item (23), `/work`'s hero (19), the Status mark,
hit-target floor, chrome and accessibility floor (17 each), the error surface (15), the rendered-output
suite with the `/work` baseline (13), the Directory, secondary surfaces and `/cv` (12 each), instrumentation, the `/projects`
redirect and the display entrance (8 each), and the contract suites. The `/work` baseline compared
unchanged, and the run left the tree clean.

**CI: green on the head.** Run 36102518282 on `82fb667`: `test`, `tokens-contract`, `fonts-contract`,
`contract-purity`, `registry-schema`, `rendered-output` and `literal-conformance` all succeeded.

**Registry verification: red on `dev` by design, green on `main`.** Run 36102518256 on `82fb667`: 33
of 34, the one failure A2's. The scheduled runs on `main` succeeded every day from 2026-09-14 to
2026-09-24 (eleven runs), on the Registry production serves.

**Lighthouse, local, in the pinned image: every assertion green.** No reading existed after
`1a5ada5`, and four packages changed `/`, `/work` and `/cv` since, so one was taken: `@lhci/cli` 0.15.1
driving Lighthouse 12.6.1, the build taken with both Umami variables empty and served by `pnpm start`,
three runs per URL against `.lighthouserc.js`, nothing uploaded (the method
`ops/hub-accessibility-pass.md` records for the 2026-09-24 re-read). **Accessibility 1.00, best
practices 1.00 and SEO 1.00 on all nine runs of `/`, `/work` and `/cv`, no failing accessibility
audit, `lhci assert` exit 0**, in 5 min 57 s. So the Operator ruling's merge condition, 0.95 or better
on `/` and `/work`, holds on the head as it stands. Performance was not read (it is not gated); DW-125
carries the 2026-09-24 figure.

**Production, observed read-only on 2026-09-25** with a browser user agent: `https://cuatro.dev/`
answers 200 and renders the Directory (`id="suite"`) with six rows and the premise's running sentence,
true there because all six are `Live` in the Registry it serves (`contract_version` 1.1.0,
`cs-tournament` `Live` at `inclusivcup.vercel.app`, `cs-tracker` at `token_contract` `1.0.0`).
`/projects` answers 301 to `/#suite`, `/recommendation` 404, `/work`, `/cv` and `/api/health` 200.

**Not exercised:** anything on the box (this host has no SSH access), `ops/deploy-remote.sh` on the
box, a screen reader, a phone, and a hover-capable pointer by hand. Those are the checks the ruling
lists as the Operator's.

## Previous-retro follow-through

The Epic 1 retrospective (`epic-1-retro-2026-08-29.md`) committed 14 items. Twelve are `done` in
`sprint-status.yaml`, each with a dated note, all closed by the Operator rulings of 2026-09-24 and the
packages that executed them. Two are not:

| Selector | Landed? | Evidence | Status it argues for |
|---|---|---|---|
| `epic-1-retro-item-6-audit-every-deploy-secret-against-the-cu` | **Partly.** Two of the three deploying repositories are proven against the current box, the third is not | `cuatro-portfolio`: `SERVER_HOST` 2026-08-17, `SERVER_USER` and `SSH_PRIVATE_KEY` 2026-08-27 (`gh secret list`), deploys 33104210047, 34728299598 and 34777712895 succeeded. `list-wheel`: deploy 36055872795 succeeded 2026-09-24. `digital-library`: all four `SERVER_*` secrets date 2026-02-19 (`gh secret list -R LuigiEspinosa/digital-library`), and its repair is the Operator's by the Operator ruling 2026-09-24 | `in-progress` |
| `epic-1-retro-item-10-extract-the-shared-probe-scaffold-into-o` | **No evidence found.** No shared module exists under `ops/`, and both probes still define the eight helpers (C2) | `ops/cs-tracker-adoption-probe.mjs:84,286,295,312,323,995,2020,2028`; `ops/daisyui-route-probe.mjs:98,224,239,270,299,910,1418,1426` | none: stays `open` |

This run is headless, so no status was written: the `in-progress` transition for item 6 is proposed
here and in Assumptions, and `--set-action-status` was not passed.

## Action items

Every item is **proposed, not applied**. This retrospective ran no dev loop, edited no project spec and
changed no story artifact. The owner is the Operator throughout, which is what a one-person estate
means; where a session does the work, the item says which.

### Before the Epic 2 merge

1. **Rule the premise's wording (DW-244), and settle the page's three counts with it.** Then add the
   Premise case E1 describes, so the next status change cannot make the sentence false silently.
   *Source: A1, E1.*
2. **Order the merge after `cs-tracker`'s 2.0.0 is live** (`ops/contract-adoption.md` Pending Operator
   action 8: move the Elixir suite's `v1.0.0` pin, push `main`, deploy), so the Registry verification is
   green when the merge runs; or rule, in writing, that the merge may ship the claim ahead of the push.
   *Source: A2.*
3. **Make the eight checks required on `main` at the merge, strict off** (DW-172, the Operator ruling
   2026-09-24). Observed on 2026-09-25: `main`'s protection reads `strict: true` and no contexts. Owner of
   the setting: the session that merges Epic 2. *Source: A2, B2.*

### Security

4. **Revoke the Twilio API key found in `Mutuo`'s 2021 history if it is still live, and review
   `cs-tracker`'s two findings** (its dev and test `secret_key_base` values): if they are the framework's
   generated development and test keys and production reads its own from the environment, publishing
   `cs-tracker` repairs the one dead Source link on a rendered row. Record both outcomes in KV-2's
   section of `ops/known-violations.md`, as a tracked row rather than prose. *Source: A3.*

### Spec reconciliation, proposed with evidence

5. **Restate UX-DR49 or record its breach.** Either give the non-3D budget a line for the framework
   runtime and restate the total (`EXPERIENCE.md:944-953`, `epics.md:699`), or open a known violation
   with a closer. Today every measured route is 57,189 to 91,829 gzipped bytes over 140,000 and nothing
   owns it. *Source: A4.*
6. **Correct `epics.md:2005-2010`** to thirty-one stories, twenty FRs, and the steps as they shipped
   (1, 2, 5 and 7 as stories, 3 inside 2-31, 4 as 2-34's gate, 6 deleted). *Source: A5.*

### Work for the dev loop

7. **Correct the three stale comments and the four stale citations** in E2, citing the `describe` by
   name rather than by line. *Source: E2.*
8. **Fix DW-21's four strings in `content/work.ts`** (`Nests.js` at `:44`, `Emal Development` at
   `:130`, and the `Javascript` and `Typescript` spellings), which have shipped on `/cv` and `/work`
   since the ledger found them on 2026-08-29. The Operator confirms the spellings. *Source: DW-21.*
9. **Move the e2e helpers into `tests/e2e/harness.ts`** as one test-only story: `probeComputed` first
   (five identical copies), then `goTo`, `plantStyle` and the `FINGERPRINTS` parse, closing DW-22,
   DW-232 and DW-49. *Source: C1, D1.*

### Process lessons

10. **Use `awaiting-operator` for a story whose remaining criteria are Operator checks**, or write a
    dated override into the board note when moving it to `done`. Reconcile 2-27 now: perform the
    screen-reader read, or amend its "board moves to `done` after it" clause to say the ruling moved it.
    *Source: F3.*
11. **Keep an independent verifier on every unattended story and package.** Inline review layers
    passed what the verifier then refused, six times in three days. *Source: F1.*
12. **Plant every new assertion, including guards added in review and fix rounds**, and record the
    planted failure beside it. *Source: F2.*
13. **Cite code by symbol or test name, never by line number, and state in a comment only a count a
    test holds.** *Source: E2, F7.*
14. **Each package names which of its new ledger entries bind the next merge.** The Operator's queue
    refilled by 17 in the two days that drained it. *Source: F5.*

## Acceptance verdict

**accepted-with-open-items**, criteria **declared** (`epics.md:838-861`, each story's criteria at
`:1998-3843`, and the four binding the redesign at `:3120-3132`). Rendered headless: this is the
machine verdict, and no human has confirmed or overridden it.

**Why not rejected.** All 31 stories are `done` (`pending_stories` is empty). The epic's four outcomes
are met in the evidence:

- **A Visitor reaches a directory of running applications and opens one.** Production has served it
  since 2026-09-13 with six `Live` rows; `dev` renders five `Live` and one `Complete` by ruling, and
  `front-door.pw.ts` and `suite-directory.pw.ts` hold both doors and the rows (337 e2e green).
- **An accurate `tech` array and a source in one hop.** Confirmed by Story 2-4, set in the voice of
  Story 2-6, corrected by Registry 1.2.0 and 1.4.0; the one hop holds on five of six rendered rows, the
  sixth private by ruling (A3).
- **The Registry cannot lie.** The schema gate is blocking in CI, and the scheduled verification runs
  daily off the box and has caught the one claim running ahead of reality (A2), which is the mechanism
  doing its job.
- **The Operator can see how Visitors move.** The three events are live on production since
  2026-09-13 and were proven there on a real session; on `dev` the reach event also carries the front
  door since 2026-09-24.

FR-37 is met: `literal-conformance` is blocking, the alias layer and `--hero-height` are deleted, and
seam S-1 stands. The FRs the epic lists are met or met as ruled (A7).

**Why not accepted.** Three open items stand between this work and a truthful production, and each is
tracked with an owner:

1. The premise says every row is running and one is not (A1, DW-244).
2. The Registry says `cs-tracker` runs Contract 2.0.0 and it does not yet (A2, action 8).
3. The eight required checks are ruled and not set (DW-172).

None is a defect in a story's delivery; each is the last step of a ruling of 2026-09-24. They are why
this verdict is not **accepted**, and they are the conditions for the merge: **the merge should not run
while any of the three is open.** The rest (A3 to A6, C, D, E2 to E4) are deferred and tracked, or
proposed above.

## Open questions

Answers here would materially change what happens next.

1. **What does the premise say over a `Complete` row?** It decides DW-244's fix and the Premise case
   that holds it. *(A1.)*
2. **Does the merge wait for `cs-tracker`'s push, or ship the claim ahead of it?** The ruling says
   "every check green"; the Registry verification is not one of the eight required checks, so this is
   the Operator's reading of that sentence. *(A2.)*
3. **Is UX-DR49 a budget the Hub is expected to meet, or a figure to restate?** Every measured route is
   over it by the framework's own JavaScript. *(A4.)*
4. **Are DW-125 and DW-127 merge conditions?** Both ship with the merge: the mobile LCP regression and
   the hidden landmark ring on the 3D door. *(E3.)*

Not resolvable from the evidence here:

5. **Whether a screen reader speaks the homepage heading as one word**, which is 2-27's own done
   condition (`ops/hub-accessibility-pass.md` action 4) and a human check.
6. **Whether the new deploy path works on the box.** It has only run against a scratch repository;
   the merge is its first real run. *(B2.)*

## Assumptions

Recorded because this run was headless and every choice below was made without the Operator.

- **The epic** is 2, from the invocation. `detect-epic --epic 2` returned `story_count: 31`,
  `pending_stories: []` and `retro_status: optional`; no unfinished story was accepted over.
- **The range** includes the fix packages of 2026-09-24 and 2026-09-25, as the ruling directs ("it
  reads what the epic produced, including today's Operator-ruled fixes"). It includes the three commits
  that closed Epic 1's Stories 1-16 and 1-19 (`b14bcbb`, `048793f`, `62e0629`), which are counted in the
  volume and judged under Epic 1's follow-through, not here.
- **The verdict** is the machine verdict, **accepted-with-open-items**, rendered with no human
  decision. Its three merge conditions are the judgment of this run.
- **The narrowings**: aggregate views derived inline (no sub-agent tool); `bmad-review` run on twelve
  files rather than the whole diff; session transcripts searched once, not read.
- **The Lighthouse reading** used the method `ops/hub-accessibility-pass.md` records for the
  2026-09-24 re-read, in the pinned image with nothing uploaded; it is a local reading, not CI's.
- **Previous-retro transitions proposed and not written:** `epic-1-retro-item-6-...` to `in-progress`.
  Item 10 stays `open`.
- **The fourteen action items** above are this run's proposals, appended to `action_items` as `open`
  with owner `Cuatro`, the name the configuration and the Epic 1 items use, under the ids
  `epic-2-retro-item-1-...` to `epic-2-retro-item-14-...` in the order above. The ids were supplied
  rather than left to the script, whose default numbers an item by its position in the whole list and
  would have started this retrospective's at 15.
- **Public record.** This file is committed to a public repository, so the secret-scan findings in A3
  and action 4 are named by type and date only, as `ops/known-violations.md` names them.

## Notes after the retrospective

**2026-09-25: merge condition (1) is met.** The Operator ruled the premise's wording on 2026-09-25
(DW-244): "[N] personal projects became one suite. Every address below is running right now, so open
one and you are using the real thing, not looking at a picture of it." `87981a0` sets it, and
`Premise.test.tsx` now fails when a rendered entry carrying an address is not `Live`, the case E1
asked for, so a status change cannot make the sentence false with the suite green. Action 1 is closed
in `sprint-status.yaml`. Of the page's three counts, the premise's and the Directory's stay as they
are; the footer's was not part of the ruling and is DW-246, which does not bind the merge.
